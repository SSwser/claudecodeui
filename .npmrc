# Prevent installing packages published less than 1 day ago.
# Protects against supply-chain attacks where a malicious package is
# published and immediately depended on before the community notices.
min-release-age=1 day
