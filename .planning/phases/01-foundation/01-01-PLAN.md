---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types/home.ts
  - src/hooks/useHomePreferences.ts
  - src/contexts/LayoutContext.tsx
  - src/hooks/useUiPreferences.ts
  - src/components/sidebar/utils/utils.ts
autonomous: true
requirements:
  - LAND-04
  - LAND-05
  - LAY-01
  - LAY-04
must_haves:
  truths:
    - "Favorites and startup behavior are persisted from one shared source of truth."
    - "Layout mode and pane assignment can be read and updated without coupling to a specific screen."
    - "Home-view state survives refresh and synchronizes across tabs."
  artifacts:
    - path: "src/types/home.ts"
      provides: "Typed contracts for home tabs, favorites, filters, startup behavior, and pane descriptors"
    - path: "src/hooks/useHomePreferences.ts"
      provides: "Shared persistence hook for favorites, startup behavior, and home metadata"
    - path: "src/contexts/LayoutContext.tsx"
      provides: "Layout provider and actions for single/dual-pane mode"
  key_links:
    - from: "src/hooks/useHomePreferences.ts"
      to: "localStorage"
      via: "hydration and persistence"
      pattern: "localStorage|getItem|setItem"
    - from: "src/contexts/LayoutContext.tsx"
      to: "src/hooks/useUiPreferences.ts"
      via: "shared preference updates"
      pattern: "setPreference|setPreferences"
---

<objective>
Establish the persistent state contracts for Phase 1 so the landing page and dual-pane shell build against stable interfaces instead of duplicating storage logic.

Purpose: Land the cross-tab persistence model required by D-Landing, D-Favorites, D-Startup, and D-Layout before UI work begins.
Output: Typed home-state contracts, shared persistence helpers, and a reusable LayoutContext.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@src/hooks/useUiPreferences.ts
@src/hooks/useProjectsState.ts
@src/components/sidebar/utils/utils.ts
@src/types/app.ts

<interfaces>
From src/hooks/useUiPreferences.ts:
```typescript
export function useUiPreferences(storageKey = 'uiPreferences') {
  return {
    preferences: state,
    setPreference,
    setPreferences,
    resetPreferences,
    dispatch,
  };
}
```

From src/types/app.ts:

```typescript
export interface ProjectSession {
  id: string;
  title?: string;
  summary?: string;
  name?: string;
  createdAt?: string;
  created_at?: string;
  updated_at?: string;
  lastActivity?: string;
  messageCount?: number;
  __provider?: SessionProvider;
  __projectName?: string;
}

export interface Project {
  name: string;
  displayName: string;
  fullPath: string;
  path?: string;
  sessions?: ProjectSession[];
}
```

</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define home-shell contracts</name>
  <files>src/types/home.ts, src/hooks/useHomePreferences.ts, src/contexts/LayoutContext.tsx</files>
  <behavior>
    - Test 1: persisted startup behavior accepts only `restore-all`, `restore-last`, or `landing`
    - Test 2: layout mode accepts only `single` or `dual` and exposes pane descriptors for the active session assignments
    - Test 3: favorite records distinguish workspace vs session entries so landing sections can render without additional branching
  </behavior>
  <action>Create the Phase 1 contracts that Plans 03-04 will consume directly. In `src/types/home.ts`, define typed models for startup behavior, home filters, favorite workspace/session cards, browser-style tabs, and pane descriptors. In `src/hooks/useHomePreferences.ts`, centralize serialization, bounds checking, and timestamp normalization for favorites and startup preferences. In `src/contexts/LayoutContext.tsx`, expose `LayoutProvider`, `useLayout`, and imperative actions for mode changes and pane assignment per D-Layout. Keep this task interface-first: no landing-page rendering yet, and no second storage key family beyond what this hook owns.</action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <done>The repo contains a single typed contract surface for favorites, startup behavior, tabs, and pane layout, and later plans can import those contracts without needing to inspect current sidebar storage internals.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Unify persisted home preferences with existing sidebar favorites</name>
  <files>src/hooks/useUiPreferences.ts, src/components/sidebar/utils/utils.ts, src/hooks/useHomePreferences.ts, src/contexts/LayoutContext.tsx</files>
  <behavior>
    - Test 1: existing starred project data hydrates into the new home preference model without data loss
    - Test 2: cross-tab updates propagate through the existing custom-event/storage sync mechanism
    - Test 3: malformed localStorage payloads fall back to safe defaults instead of crashing the app
  </behavior>
  <action>Refactor the existing `starredProjects` helpers in `src/components/sidebar/utils/utils.ts` so Phase 1 uses one persistence layer for favorites rather than parallel storage schemes. Extend `src/hooks/useUiPreferences.ts` only where it still needs to own generic UI toggles; move startup behavior, landing favorites, and layout mode/pane metadata into `src/hooks/useHomePreferences.ts` and `src/contexts/LayoutContext.tsx`. Preserve current project starring during migration, keep everything localStorage-backed unless a hard blocker appears, and reuse the existing storage/custom-event synchronization pattern rather than inventing polling.</action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <done>Favorites, startup behavior, and layout preferences hydrate from a shared persistence path, existing starred project users are migrated in-place, and invalid stored values no longer break Phase 1 UI boot.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client -> localStorage | Untrusted persisted data is rehydrated into the home shell |
| home preferences -> view state | Corrupt values could break routing, tab restoration, or pane assignment |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01-01 | T | src/hooks/useHomePreferences.ts | mitigate | Parse persisted payloads through explicit runtime guards, default unknown enum values, and cap favorite list sizes before saving or hydrating |
| T-01-01-02 | D | src/contexts/LayoutContext.tsx | mitigate | Reject invalid pane descriptors and fall back to `single` mode when required session references are missing |
| T-01-01-03 | I | src/components/sidebar/utils/utils.ts | accept | Local favorites are non-sensitive metadata; no additional secrecy layer is needed beyond not storing message bodies |
</threat_model>

<verification>
Run `npm run typecheck` after each task. Manually inspect browser localStorage only if type errors suggest migration logic drift.
</verification>

<success_criteria>
Phase 1 executors have one stable contract surface for startup behavior, favorites, tabs, and layout state, and Plans 03-04 can consume that surface without reworking persistence.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-foundation-01-SUMMARY.md`
</output>
