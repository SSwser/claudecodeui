---
phase: 01-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - server/routes/projects.js
  - server/index.js
  - src/utils/api.js
  - src/components/project-creation-wizard/types.ts
  - src/components/project-creation-wizard/ProjectCreationWizard.tsx
  - src/components/project-creation-wizard/components/StepTypeSelection.tsx
  - src/components/project-creation-wizard/components/StepConfiguration.tsx
  - src/components/project-creation-wizard/components/StepReview.tsx
autonomous: true
requirements:
  - LAND-07
must_haves:
  truths:
    - "Landing-page actions can open a workspace creation flow that defaults to logical workspaces."
    - "Users can opt into a worktree-backed workspace without breaking existing import/clone flows."
    - "Workspace deletion and association flows surface filesystem consequences before destructive actions."
  artifacts:
    - path: "server/routes/projects.js"
      provides: "Workspace creation lifecycle for logical and worktree-backed modes"
    - path: "src/components/project-creation-wizard/ProjectCreationWizard.tsx"
      provides: "Phase 1 workspace creation UX"
    - path: "src/utils/api.js"
      provides: "Client contract for the revised workspace payload"
  key_links:
    - from: "src/components/project-creation-wizard/ProjectCreationWizard.tsx"
      to: "/api/projects/create-workspace"
      via: "createWorkspaceRequest"
      pattern: "createWorkspace"
    - from: "server/routes/projects.js"
      to: "git/worktree lifecycle"
      via: "workspace mode branching"
      pattern: "workspaceType|worktree|branch"
---

<objective>
Upgrade the existing workspace creation flow so Phase 1 can launch new workspace actions from landing while honoring the locked workspace-mode decisions.

Purpose: Make the landing-page "new workspace" action real instead of a placeholder, and keep the API consistent with the logical-vs-worktree split from the context decisions.
Output: Revised workspace API payload, updated wizard flow, and safe lifecycle handling for worktree-backed workspaces.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@src/components/project-creation-wizard/ProjectCreationWizard.tsx
@src/utils/api.js
@server/routes/projects.js
@server/index.js

<interfaces>
From src/utils/api.js:
```javascript
createWorkspace: (workspaceData) =>
  authenticatedFetch('/api/projects/create-workspace', {
    method: 'POST',
    body: JSON.stringify(workspaceData),
  })
```

From server/routes/projects.js:

```javascript
router.post('/create-workspace', async (req, res) => {
  const { workspaceType, path: workspacePath, githubUrl, githubTokenId, newGithubToken } = req.body;
});
```

</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend the workspace API for logical and worktree-backed modes</name>
  <files>server/routes/projects.js, server/index.js, src/utils/api.js</files>
  <behavior>
    - Test 1: `logical` mode stores/returns workspace metadata without requiring git or cloning
    - Test 2: `worktree` mode accepts branch/base inputs and rejects unsafe filesystem operations
    - Test 3: legacy create/import payloads continue to work after route-boundary normalization
  </behavior>
  <action>Revise the workspace contract so the route accepts an explicit workspace mode per the Phase 1 decisions: logical by default, optional worktree-backed mode with branch/base information. Normalize old `existing`/`new` payloads at the route boundary so current callers do not break while the landing UI is being introduced. Keep path validation in place, make worktree creation/association explicit rather than implicit, and add the deletion/association hooks needed for later UI prompts without auto-importing arbitrary worktrees.</action>
  <verify>
    <automated>node --check server/routes/projects.js && node --check server/index.js</automated>
  </verify>
  <done>The server can distinguish logical vs worktree-backed workspace creation safely, and the client API exposes a stable contract for the new landing action without regressing the current project wizard flow.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Update the workspace wizard for the new mode decisions</name>
  <files>src/components/project-creation-wizard/types.ts, src/components/project-creation-wizard/ProjectCreationWizard.tsx, src/components/project-creation-wizard/components/StepTypeSelection.tsx, src/components/project-creation-wizard/components/StepConfiguration.tsx, src/components/project-creation-wizard/components/StepReview.tsx</files>
  <behavior>
    - Test 1: the default mode shown to users is logical workspace
    - Test 2: branch/base controls appear only when worktree mode is selected
    - Test 3: review and destructive follow-up copy explains the filesystem consequences of worktree mode clearly
  </behavior>
  <action>Rework the existing wizard so it can be launched from Phase 1 landing actions as "New Workspace" while still supporting import/clone paths. Rename and reorder options to match the context decisions, keep logical workspace selected by default, and only reveal worktree-specific controls when the user opts in. The review step must make the resulting filesystem path/branch behavior explicit, and the wizard must preserve the current project import path rather than hiding it behind the new defaults.</action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <done>The workspace wizard matches the logical/worktree decision model, remains backward compatible with current project creation flows, and produces the payload that Plan 03 can call from the landing page without additional translation code.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client -> /api/projects/create-workspace | User-controlled workspace mode, path, and branch data crosses into filesystem operations |
| server -> git/worktree operations | Unsafe defaults could create or delete repository state unexpectedly |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-02-01 | T | server/routes/projects.js | mitigate | Reuse existing path validation, normalize mode values server-side, and reject worktree operations when required git inputs are missing |
| T-01-02-02 | E | server/index.js | mitigate | Keep workspace creation behind `authenticateToken` and do not expose destructive worktree deletion without an explicit user-confirmed flag |
| T-01-02-03 | R | src/components/project-creation-wizard/StepReview.tsx | mitigate | Surface branch/base/path summary in the review step so users can verify intent before submitting destructive or repo-mutating actions |
</threat_model>

<verification>
Run `node --check server/routes/projects.js && node --check server/index.js` after the route task, then `npm run typecheck` after the wizard task.
</verification>

<success_criteria>
Phase 1 has a concrete, backward-compatible workspace creation flow that matches the context decisions and can be launched directly from the landing page.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-foundation-02-SUMMARY.md`
</output>
