---
phase: 01-foundation
plan: 03
type: execute
wave: 2
depends_on:
  - 01
  - 02
files_modified:
  - src/components/ui/button.tsx
  - src/components/ui/input.tsx
  - src/components/ui/dialog.tsx
  - src/components/ui/select.tsx
  - src/components/home/view/LandingPage.tsx
  - src/components/home/view/FavoritesSection.tsx
  - src/components/home/view/RecentSessionsList.tsx
  - src/components/home/view/HomeFilters.tsx
  - src/components/app/AppContent.tsx
  - src/hooks/useProjectsState.ts
  - src/components/main-content/view/subcomponents/MainContentStateView.tsx
  - src/i18n/locales/en/common.json
  - src/i18n/locales/zh-CN/common.json
autonomous: true
requirements:
  - LAND-01
  - LAND-02
  - LAND-03
  - LAND-04
  - LAND-05
  - LAND-06
  - LAND-07
must_haves:
  truths:
    - "App launch can land on a welcoming home screen instead of forcing direct session entry."
    - "Users can search, filter, favorite, and open recent workspace/session items from the landing page."
    - "New session and new workspace actions are visible from the landing page."
  artifacts:
    - path: "src/components/home/view/LandingPage.tsx"
      provides: "Landing page composition for search, filters, favorites, and recent activity"
    - path: "src/components/home/view/FavoritesSection.tsx"
      provides: "Split favorites display for workspaces and sessions"
    - path: "src/components/home/view/RecentSessionsList.tsx"
      provides: "Timeline-style recent session list with status indicators"
    - path: "src/components/app/AppContent.tsx"
      provides: "Startup routing between landing and active session contexts"
  key_links:
    - from: "src/components/home/view/LandingPage.tsx"
      to: "src/hooks/useProjectsState.ts"
      via: "derived recent/favorite/filter data"
      pattern: "recent|favorite|filter"
    - from: "src/components/home/view/LandingPage.tsx"
      to: "src/components/project-creation-wizard/ProjectCreationWizard.tsx"
      via: "new workspace action"
      pattern: "ProjectCreationWizard|showNew"
    - from: "src/components/app/AppContent.tsx"
      to: "src/components/home/view/LandingPage.tsx"
      via: "startup behavior and empty/home routing"
      pattern: "LandingPage|startup"
---

<objective>
Build the Phase 1 landing experience and wire startup routing so users can enter the app through home, restore state according to preference, and quickly jump into sessions or workspace creation.

Purpose: Deliver the visible Foundation outcome: a welcoming landing page with recent activity, split favorites, search/filter controls, and entry actions.
Output: shadcn-based landing components, startup routing integration, and localized home copy.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@src/components/app/AppContent.tsx
@src/hooks/useProjectsState.ts
@src/components/main-content/view/MainContent.tsx
@src/components/project-creation-wizard/ProjectCreationWizard.tsx
@src/shared/view/ui/Button.tsx
@src/shared/view/ui/Input.tsx

<interfaces>
From src/components/app/AppContent.tsx:
```typescript
<MainContent
  selectedProject={selectedProject}
  selectedSession={selectedSession}
  activeTab={activeTab}
  setActiveTab={setActiveTab}
  onNavigateToSession={(targetSessionId: string) => navigate(`/session/${targetSessionId}`)}
/>
```

From src/hooks/useProjectsState.ts:

```typescript
export function useProjectsState({ sessionId, navigate, latestMessage, isMobile, activeSessions }: UseProjectsStateArgs) {
  return {
    selectedProject,
    selectedSession,
    activeTab,
    sidebarOpen,
    isLoadingProjects,
    refreshProjectsSilently,
    sidebarSharedProps,
  };
}
```

</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build the landing page with shadcn primitives and split favorites</name>
  <files>src/components/ui/button.tsx, src/components/ui/input.tsx, src/components/ui/dialog.tsx, src/components/ui/select.tsx, src/components/home/view/LandingPage.tsx, src/components/home/view/FavoritesSection.tsx, src/components/home/view/RecentSessionsList.tsx, src/components/home/view/HomeFilters.tsx, src/i18n/locales/en/common.json, src/i18n/locales/zh-CN/common.json</files>
  <behavior>
    - Test 1: the home screen renders search plus cascading project/workspace/type filters per the context decisions
    - Test 2: favorites render as split workspace/session sections with capped default counts and overflow dialog support
    - Test 3: recent sessions render as a timeline list with visible status indicators and auxiliary project metadata
  </behavior>
  <action>Copy the Phase 1 shadcn/ui primitives needed for the landing page (`Button`, `Input`, `Dialog`, `Select`) into `src/components/ui/` and use them exclusively in the new home components. Build the landing surface in `src/components/home/view/` with the Google-Drive-inspired layout from context: top search/filter bar, split favorites, recent activity timeline, and clear empty states. Keep projects as auxiliary metadata rather than the primary grouping model, and limit the first-pass localization work to the app's existing primary locales in `en` and `zh-CN` while reusing the current i18n namespaces cleanly.</action>
  <verify>
    <automated>npm run typecheck && npm run build</automated>
  </verify>
  <done>The repo contains a rendered landing page built with shadcn-style primitives, split favorites and recent activity honor the Phase 1 decisions, and home copy is localized for the primary app locales.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire startup routing and landing actions into the app shell</name>
  <files>src/components/app/AppContent.tsx, src/hooks/useProjectsState.ts, src/components/main-content/view/subcomponents/MainContentStateView.tsx, src/components/home/view/LandingPage.tsx</files>
  <behavior>
    - Test 1: startup behavior defaults to full restore but can switch to last-active-only or always-landing
    - Test 2: clicking a recent or favorite entry opens that session/workspace context directly
    - Test 3: the landing page exposes visible new-session and new-workspace actions without breaking direct `/session/:sessionId` navigation
  </behavior>
  <action>Use the Plan 01 persistence contracts to route startup through landing or restored sessions without regressing direct route entry. Extend `useProjectsState.ts` to derive the recent items, favorites, and filtered home collections that `LandingPage.tsx` needs, then mount the landing page from `AppContent.tsx` and `MainContentStateView.tsx` so home appears when no session is selected or the startup preference requires it. New session and new workspace actions must be first-class controls on the landing page, and direct `/session/:sessionId` navigation must still materialize the requested session immediately.</action>
  <verify>
    <automated>npm run typecheck && npm run build</automated>
  </verify>
  <done>App launch now respects the startup preference, the landing page is the primary Foundation entry surface, and users can open existing contexts or create new ones from home without breaking legacy session routes.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| app shell -> landing/home state | Startup routing decisions now choose between restored sessions and the landing screen |
| search/filter UI -> session/project metadata | User-entered queries and stored metadata drive filtering and navigation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03-01 | T | src/hooks/useProjectsState.ts | mitigate | Normalize derived home data from trusted project/session structures and never render raw unbounded HTML from session snippets |
| T-01-03-02 | D | src/components/home/view/LandingPage.tsx | mitigate | Cap default favorite/recent counts and debounce search/filter work to keep landing render time within the Phase 1 performance budget |
| T-01-03-03 | S | src/components/app/AppContent.tsx | mitigate | Preserve direct session-route selection so a stale startup preference cannot spoof the requested explicit session navigation target |
</threat_model>

<verification>
Run `npm run typecheck && npm run build` after each task. Confirm the build still emits a valid SPA bundle and the landing page compiles with the new UI primitives.
</verification>

<success_criteria>
The app presents a landing page on launch when appropriate, shows recent/favorite data in the agreed visual structure, and exposes direct open/create actions without regressing existing session entry behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-foundation-03-SUMMARY.md`
</output>
