---
phase: 01-foundation
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/app/AppContent.tsx
  - src/hooks/useAppTabs.ts
  - src/hooks/useHomePreferences.ts
  - src/types/home.ts
  - src/components/app/view/AppTabStrip.tsx
  - src/components/main-content/view/MainContent.tsx
  - src/components/main-content/view/subcomponents/MainContentStateView.tsx
  - src/components/app/AppContent.test.tsx
  - src/hooks/useAppTabs.test.ts
autonomous: true
gap_closure: true
requirements:
  - LAND-01
  - LAND-05
  - LAND-06
  - LAY-01
  - LAY-04
must_haves:
  truths:
    - 'Unauthenticated users still hit auth first, but successful auth on the root route resolves to landing when no explicit session route is requested.'
    - 'Landing renders as a chrome-light surface with no desktop sidebar leakage and no shell tab strip leakage.'
    - 'Returning Home after creating or opening sessions clears stale shell residue instead of leaving transient tabs behind.'
  artifacts:
    - path: 'src/components/app/AppContent.tsx'
      provides: 'Single source of truth for auth -> root-route -> landing or shell resolution'
    - path: 'src/hooks/useAppTabs.ts'
      provides: 'Deterministic home/session shell transitions and stale-tab cleanup rules'
    - path: 'src/hooks/useHomePreferences.ts'
      provides: 'Safe persistence normalization for shell tab hydration'
  key_links:
    - from: 'src/components/app/AppContent.tsx'
      to: 'src/hooks/useAppTabs.ts'
      via: 'resolved shell state instead of ad-hoc local rootViewMode branches'
      pattern: 'useAppTabs|rootViewMode|showLandingPage'
    - from: 'src/hooks/useAppTabs.ts'
      to: 'src/hooks/useHomePreferences.ts'
      via: 'persisted shellTabs and activeShellTabId cleanup'
      pattern: 'setShellTabs|setActiveShellTabId'
---

<objective>
Close the UAT gaps around launch/login routing, landing chrome leakage, and Home lifecycle by making the root-shell state machine deterministic again.

Purpose: Phase 01 currently has landing data and shell-tab utilities, but the live app path still resolves launch, sidebar visibility, and Home return behavior through split local state. This plan collapses those transitions into one reliable controller so landing behaves like a real mode instead of a leftover empty shell.
Output: Root-route landing resolution, chrome-light landing rendering, and stable Home/session lifecycle with regression coverage.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-UAT.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-UI-SPEC.md
@.planning/phases/01-foundation/01-foundation-03-SUMMARY.md
@.planning/phases/01-foundation/01-foundation-04-SUMMARY.md
@src/components/app/AppContent.tsx
@src/hooks/useAppTabs.ts
@src/hooks/useHomePreferences.ts
@src/components/main-content/view/MainContent.tsx
@src/components/app/view/AppTabStrip.tsx

<interfaces>
From src/components/app/AppContent.tsx:
```tsx
const [rootViewMode, setRootViewMode] = useState<'landing' | 'empty'>('landing');
const showDesktopSidebar = Boolean(sessionId || selectedSession || rootViewMode === 'empty');
```

From src/hooks/useAppTabs.ts:

```ts
const [rootViewMode, setRootViewMode] = useState<RootViewMode>('landing');
return {
  shellTabs,
  activeShellTabId: preferences.activeShellTabId,
  rootViewMode,
  setRootViewMode,
  selectShellTab,
  closeShellTab,
};
```

From .planning/phases/01-foundation/01-UAT.md:

```md
- 启动先到登录界面，登录成功之后才是 landing page；现在 landing page 还是有侧边栏。
- new session 之后会显示 home 的 tab，点击 home 返回 landing page 后，tab 没消失。
```

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reconcile auth, root-route startup, and chrome-light landing state</name>
  <files>src/components/app/AppContent.tsx, src/components/main-content/view/MainContent.tsx, src/components/main-content/view/subcomponents/MainContentStateView.tsx, src/components/app/AppContent.test.tsx</files>
  <read_first>
    - .planning/phases/01-foundation/01-UAT.md
    - .planning/phases/01-foundation/01-UI-SPEC.md
    - src/components/app/AppContent.tsx
    - src/components/main-content/view/MainContent.tsx
  </read_first>
  <action>Replace the split landing-state handling in AppContent with one root-shell decision path that respects the UI-spec adjustment contract. Keep auth as the first gate, but after successful auth on `/` with no explicit `/session/:sessionId` route, resolve to landing whenever the user does not have a valid explicit restore target. Do not blindly navigate to `lastOpenedSessionId` when the startup preference is `landing`, when the restored tab payload is malformed, or when the session no longer exists. Compute desktop sidebar visibility and shell-tab visibility from the resolved shell mode rather than `Boolean(sessionId || selectedSession || rootViewMode === 'empty')`, so landing becomes a true chrome-light state with no desktop sidebar leakage and no shell tab strip leakage. Add focused regression coverage that exercises post-auth root navigation and verifies landing renders without shell chrome.</action>
  <verify>
    <automated>npx vitest run src/components/app/AppContent.test.tsx && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>Logging in from the root route lands on landing when there is no explicit session route, landing does not show desktop sidebar or shell tabs, and a broken persisted restore target falls back conservatively instead of trapping the user in an empty shell.</acceptance_criteria>
  <done>Root launch behavior matches the diagnosed UAT expectation and landing is rendered as a dedicated mode instead of a partially hidden shell.</done>
</task>

<task type="auto">
  <name>Task 2: Normalize Home selection, add-tab fallback, and stale shell-tab cleanup</name>
  <files>src/hooks/useAppTabs.ts, src/hooks/useHomePreferences.ts, src/types/home.ts, src/components/app/view/AppTabStrip.tsx, src/hooks/useAppTabs.test.ts, src/components/app/AppContent.tsx</files>
  <read_first>
    - .planning/phases/01-foundation/01-UAT.md
    - src/hooks/useAppTabs.ts
    - src/hooks/useHomePreferences.ts
    - src/components/app/view/AppTabStrip.tsx
  </read_first>
  <action>Treat Home as a deterministic landing transition, not as a session-like shell tab that can linger after navigation. Update `useAppTabs.ts` so selecting Home, closing the last session tab, and returning to `/` all clear or normalize `shellTabs`, `activeShellTabId`, and `rootViewMode` in one atomic transition. Drop orphaned restored tabs whose `sessionId` no longer resolves, and prevent landing from inheriting transient shell residue after `new session` then `Home`. Keep add-tab behavior explicit per the UI-spec contract: if a project context exists, it may create a new session draft; if no project context exists, it must route back into the landing create flow instead of persisting a stale empty tab. Add regression coverage for Home-return cleanup and stale persisted-tab normalization.</action>
  <verify>
    <automated>npx vitest run src/hooks/useAppTabs.test.ts && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>Selecting Home after creating or opening a session removes stale shell residue, add-tab on landing does not manufacture a ghost tab, and invalid persisted shell tabs are normalized away during hydration.</acceptance_criteria>
  <done>Home/session lifecycle is stable across create, open, close, and return-to-landing flows, with no stale tab persistence left behind.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                       | Description                                                                                      |
| ---------------------------------------------- | ------------------------------------------------------------------------------------------------ |
| persisted home preferences -> live shell state | Untrusted localStorage data can incorrectly force the app into shell mode or resurrect dead tabs |
| auth success -> root route resolution          | A bad startup branch can strand users in the wrong mode even when the route is `/`               |

## STRIDE Threat Register

| Threat ID  | Category | Component                         | Disposition | Mitigation Plan                                                                                                                             |
| ---------- | -------- | --------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------- |
| T-01-05-01 | T        | src/hooks/useHomePreferences.ts   | mitigate    | Normalize persisted shell tabs and active IDs before hydration, dropping tabs that do not point to a valid session or mode                  |
| T-01-05-02 | D        | src/components/app/AppContent.tsx | mitigate    | Prefer conservative landing fallback whenever restore intent is invalid so startup cannot dead-end into empty shell chrome                  |
| T-01-05-03 | S        | src/hooks/useAppTabs.ts           | mitigate    | Route explicit `/session/:sessionId` intent ahead of restored root-state defaults so landing logic never overrides a direct session request |

</threat_model>

<verification>
Run the two focused Vitest suites plus `npm run typecheck`. During execution, keep one browser smoke pass only after both automated checks are green.
</verification>

<success_criteria>
The two diagnosed UAT gaps around launch/login-to-landing behavior and Home-tab lifecycle are closed at the state-machine level, not just patched visually.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-foundation-05-SUMMARY.md`
</output>
