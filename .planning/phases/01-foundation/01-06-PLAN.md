---
phase: 01-foundation
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/project-creation-wizard/ProjectCreationWizard.tsx
  - src/components/project-creation-wizard/components/StepTypeSelection.tsx
  - src/components/project-creation-wizard/components/StepConfiguration.tsx
  - src/components/project-creation-wizard/components/StepReview.tsx
  - src/components/project-creation-wizard/types.ts
  - src/components/project-creation-wizard/utils/pathUtils.ts
  - src/components/project-creation-wizard/data/workspaceApi.ts
  - src/i18n/locales/en/common.json
  - src/i18n/locales/zh-CN/common.json
  - server/routes/projects.js
  - src/components/project-creation-wizard/ProjectCreationWizard.test.tsx
autonomous: true
gap_closure: true
requirements:
  - LAND-06
  - LAND-07
must_haves:
  truths:
    - 'Logical workspace mode shows the logical-path and optional clone flow only when it is actually relevant.'
    - 'Worktree mode requires workspace path, source path, and branch name before the flow can continue.'
    - 'The review step mirrors the literal payload that will be submitted, so mode intent does not silently change between UI and server.'
  artifacts:
    - path: 'src/components/project-creation-wizard/ProjectCreationWizard.tsx'
      provides: 'Single owner of workspace wizard validation and step transitions'
    - path: 'src/components/project-creation-wizard/components/StepConfiguration.tsx'
      provides: 'Mode-gated field rendering and validation messaging'
    - path: 'server/routes/projects.js'
      provides: 'Server normalization that matches the wizard payload exactly'
  key_links:
    - from: 'src/components/project-creation-wizard/ProjectCreationWizard.tsx'
      to: 'src/components/project-creation-wizard/data/workspaceApi.ts'
      via: 'submitted create-workspace payload'
      pattern: 'createWorkspaceRequest|workspaceType|sourcePath|branchName'
    - from: 'src/components/project-creation-wizard/components/StepReview.tsx'
      to: 'server/routes/projects.js'
      via: 'reviewed values must match normalized route inputs'
      pattern: 'workspaceType|path|sourcePath|branchName|baseBranch'
---

<objective>
Close the UAT failures where logical and worktree workspace creation no longer match the agreed Phase 01 flow.

Purpose: The existing wizard and server support both logical and worktree concepts, but the diagnosed UAT says the flow is now "completely wrong" for both modes. This plan realigns field gating, validation, review, and submitted payloads so the UI and backend describe the same operation.
Output: Mode-correct workspace wizard behavior, exact review payload mirroring, and server validation that matches the frontend contract.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation/01-UAT.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-UI-SPEC.md
@.planning/phases/01-foundation/01-foundation-02-SUMMARY.md
@src/components/project-creation-wizard/ProjectCreationWizard.tsx
@src/components/project-creation-wizard/components/StepConfiguration.tsx
@src/components/project-creation-wizard/components/StepReview.tsx
@src/components/project-creation-wizard/types.ts
@src/components/project-creation-wizard/utils/pathUtils.ts
@server/routes/projects.js

<interfaces>
From src/components/project-creation-wizard/types.ts:
```ts
export type WorkspaceType = 'logical' | 'worktree';

export type CreateWorkspacePayload = {
workspaceType: WorkspaceType;
path: string;
sourcePath?: string;
branchName?: string;
baseBranch?: string;
githubUrl?: string;
githubTokenId?: string;
newGithubToken?: string;
};

````

From .planning/phases/01-foundation/01-UI-SPEC.md:
```md
- logical mode requires workspace path; optional github clone fields only when URL exists.
- worktree mode requires workspace path + source path + branch name.
- Review step mirrors submitted payload exactly.
````

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Realign wizard field gating, validation, and explanatory copy to the locked mode contract</name>
  <files>src/components/project-creation-wizard/ProjectCreationWizard.tsx, src/components/project-creation-wizard/components/StepTypeSelection.tsx, src/components/project-creation-wizard/components/StepConfiguration.tsx, src/components/project-creation-wizard/types.ts, src/components/project-creation-wizard/utils/pathUtils.ts, src/i18n/locales/en/common.json, src/i18n/locales/zh-CN/common.json, src/components/project-creation-wizard/ProjectCreationWizard.test.tsx</files>
  <read_first>
    - .planning/phases/01-foundation/01-UAT.md
    - .planning/phases/01-foundation/01-UI-SPEC.md
    - src/components/project-creation-wizard/ProjectCreationWizard.tsx
    - src/components/project-creation-wizard/components/StepConfiguration.tsx
  </read_first>
  <action>Make Step 2 strictly mode-gated per the UI-spec adjustment contract. In logical mode, require only workspace path, keep GitHub clone/auth fields hidden until a URL is present, and ensure validation copy tells the user what they must provide next instead of only saying something is missing. In worktree mode, require workspace path, source path, and branch name before advancing; keep base branch optional with an explicit default that is visible to the user. Remove any implicit step advancement or mode leakage that makes one mode inherit fields from the other. Add focused regression coverage for logical vs worktree field visibility and validation messaging so the UAT failure cannot recur silently.</action>
  <verify>
    <automated>npx vitest run src/components/project-creation-wizard/ProjectCreationWizard.test.tsx && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>Logical mode exposes only its required fields plus optional clone controls when a URL exists, worktree mode blocks progression until path/source/branch are present, and both locales explain the next required user action clearly.</acceptance_criteria>
  <done>The wizard's rendered fields and client-side validation now match the Phase 01 contract for both logical and worktree flows.</done>
</task>

<task type="auto">
  <name>Task 2: Make the review step and server normalization mirror the submitted workspace payload exactly</name>
  <files>src/components/project-creation-wizard/components/StepReview.tsx, src/components/project-creation-wizard/data/workspaceApi.ts, server/routes/projects.js, src/components/project-creation-wizard/ProjectCreationWizard.tsx</files>
  <read_first>
    - src/components/project-creation-wizard/components/StepReview.tsx
    - src/components/project-creation-wizard/data/workspaceApi.ts
    - server/routes/projects.js
    - .planning/phases/01-foundation/01-foundation-02-SUMMARY.md
  </read_first>
  <action>Audit the exact payload path from wizard state to `createWorkspaceRequest` to `normalizeWorkspacePayload`, then remove any silent coercion that changes the user's intended mode or hides fields from the review screen. The review step must show the literal values that will be submitted, including whether this is logical vs worktree, the effective workspace path, optional clone source, worktree source path, branch name, and base branch default. On the server, keep token secrets out of URL-based error messages, continue validating path safety, and reject worktree requests that arrive without `sourcePath` or `branchName` even if a buggy client submits them. Preserve backward compatibility only at the route boundary; do not let legacy normalization blur the review semantics of the modern wizard.</action>
  <verify>
    <automated>node --check server/routes/projects.js && npx vitest run src/components/project-creation-wizard/ProjectCreationWizard.test.tsx && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>The review step is a truthful preview of the outgoing payload, logical clone flows do not leak auth material into visible URLs or messages, and the server enforces the same mode requirements the wizard promises.</acceptance_criteria>
  <done>Logical and worktree workspace flows are aligned end to end across UI, review, request payload, and server validation.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                    | Description                                                                              |
| ------------------------------------------- | ---------------------------------------------------------------------------------------- |
| wizard form state -> create-workspace API   | User-entered filesystem and git data crosses into server-side workspace operations       |
| server normalization -> git/path operations | Silent mode coercion could create the wrong type of workspace or hide destructive intent |

## STRIDE Threat Register

| Threat ID  | Category | Component                                                        | Disposition | Mitigation Plan                                                                                                                   |
| ---------- | -------- | ---------------------------------------------------------------- | ----------- | --------------------------------------------------------------------------------------------------------------------------------- |
| T-01-06-01 | T        | server/routes/projects.js                                        | mitigate    | Reject incomplete worktree payloads server-side and keep route normalization conservative instead of inferring missing git inputs |
| T-01-06-02 | I        | src/components/project-creation-wizard/components/StepReview.tsx | mitigate    | Do not echo raw secrets; show token mode/auth strategy without rendering token values or embedding them in URLs                   |
| T-01-06-03 | R        | src/components/project-creation-wizard/ProjectCreationWizard.tsx | mitigate    | Make review output mirror the submitted payload exactly so users can verify intent before filesystem or git operations run        |

</threat_model>

<verification>
Run the focused wizard Vitest suite, `node --check server/routes/projects.js`, and `npm run typecheck`. If the executor adds route-level tests, keep them scoped to the create-workspace contract only.
</verification>

<success_criteria>
Both UAT workspace-flow failures are closed because the wizard once again reflects the intended logical/worktree model from configuration through submission.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-foundation-06-SUMMARY.md`
</output>
