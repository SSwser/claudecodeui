---
phase: 01-foundation
plan: 07
type: execute
wave: 2
depends_on:
  - 05
files_modified:
  - src/components/app/AppContent.tsx
  - src/hooks/useAppTabs.ts
  - src/contexts/LayoutContext.tsx
  - src/hooks/useHomePreferences.ts
  - src/components/app/view/AppTabStrip.tsx
  - src/components/app/view/TabContextMenu.tsx
  - src/components/app/view/LayoutSwitcher.tsx
  - src/components/app/view/PaneDropZone.tsx
  - src/components/main-content/view/MainContent.tsx
  - src/components/main-content/view/subcomponents/MainContentHeader.tsx
  - src/components/main-content/types/types.ts
  - src/types/home.ts
  - src/components/app/AppContent.test.tsx
  - src/contexts/LayoutContext.test.tsx
autonomous: false
gap_closure: true
requirements:
  - LAND-05
  - LAY-01
  - LAY-02
  - LAY-03
  - LAY-04
  - LAY-05
must_haves:
  truths:
    - 'Opening an existing session from landing, sidebar, or route intent creates or reuses a stable shell tab instead of flickering.'
    - 'Closing tabs updates active selection and route atomically, and users can still open existing sessions after closing others.'
    - 'Dual-pane mode actually renders two independent pane contexts, clears the secondary pane when returning to single mode, and never shares pane content-tab state by accident.'
  artifacts:
    - path: 'src/components/app/AppContent.tsx'
      provides: 'Live shell integration for AppTabStrip, LayoutSwitcher, and pane rendering'
    - path: 'src/hooks/useAppTabs.ts'
      provides: 'Stable session-tab open/select/close semantics used by all entry points'
    - path: 'src/contexts/LayoutContext.tsx'
      provides: 'Validated single/dual-pane assignment and per-pane content-tab state'
  key_links:
    - from: 'src/components/app/AppContent.tsx'
      to: 'src/components/app/view/AppTabStrip.tsx'
      via: 'live shell tab strip mount and handlers'
      pattern: 'AppTabStrip|onSelectTab|onCloseTab|onAddTab'
    - from: 'src/components/app/view/TabContextMenu.tsx'
      to: 'src/contexts/LayoutContext.tsx'
      via: 'open-in-new-pane explicit session assignment'
      pattern: 'Open in New Pane|assignSessionToPane'
    - from: 'src/components/main-content/view/MainContent.tsx'
      to: 'src/contexts/LayoutContext.tsx'
      via: 'per-pane active content tab and session rendering'
      pattern: 'layoutMode|activePane|setPaneContentTab'
---

<objective>
Close the blocker UAT failures around tab flicker, inability to reopen existing sessions, broken close behavior, and non-functional dual-pane mode.

Purpose: Phase 01 already contains shell-tab and layout artifacts, but they are not fully connected to the live app path. This plan reconnects those artifacts to the running shell and stabilizes the route/tab/pane transitions that are currently failing in UAT.
Output: Live AppTabStrip integration, stable open/select/close behavior for existing sessions, and a working dual-pane shell with independent pane state.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation/01-UAT.md
@.planning/phases/01-foundation/01-UI-SPEC.md
@.planning/phases/01-foundation/01-foundation-04-SUMMARY.md
@.planning/phases/01-foundation/01-05-PLAN.md
@src/components/app/AppContent.tsx
@src/hooks/useAppTabs.ts
@src/contexts/LayoutContext.tsx
@src/components/app/view/AppTabStrip.tsx
@src/components/app/view/TabContextMenu.tsx
@src/components/app/view/LayoutSwitcher.tsx
@src/components/app/view/PaneDropZone.tsx
@src/components/main-content/view/MainContent.tsx

<interfaces>
From src/components/app/view/AppTabStrip.tsx:
```tsx
type AppTabStripProps = {
  tabs: AppShellTab[];
  activeTabId: string;
  onSelectTab: (tabId: string) => void;
  onCloseTab: (tabId: string) => void;
  onAddTab: () => void;
};
```

From .planning/phases/01-foundation/01-UAT.md:

```md
- tab 标签一直闪烁；点击加号只能新建 session，不能从已有 session 打开；关闭也用不了。
- 分屏还是一样用不了。
```

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reconnect the live shell to AppTabStrip and stabilize open/select/close transitions</name>
  <files>src/components/app/AppContent.tsx, src/hooks/useAppTabs.ts, src/components/app/view/AppTabStrip.tsx, src/components/app/view/TabContextMenu.tsx, src/components/main-content/types/types.ts, src/components/app/AppContent.test.tsx</files>
  <read_first>
    - .planning/phases/01-foundation/01-UAT.md
    - src/components/app/AppContent.tsx
    - src/hooks/useAppTabs.ts
    - src/components/app/view/AppTabStrip.tsx
    - src/components/app/view/TabContextMenu.tsx
  </read_first>
  <action>Mount the existing shell-tab components into the live app path instead of leaving them as disconnected artifacts. `AppContent.tsx` must source the shell strip from `useAppTabs.ts`, route all open-session intents through the same open-or-select path, and stop letting route changes, landing actions, and sidebar/session selection fight each other. Extend `useAppTabs.ts` so opening an existing session reuses the matching tab by `sessionId` when present, inserts a new tab predictably when absent, and updates route plus active tab in one transition to eliminate flicker. Expand `TabContextMenu.tsx` beyond close-only behavior so the shell can expose explicit actions required by the UI-spec contract, including `Open in New Pane` for the next task. Keep add-tab semantics precise: with project context it can create a new session draft; without project context it must send the user back into the landing create/open flow, not a dead shell. Add regression coverage for open-existing-session, close-active-tab, and reopen-after-close flows.</action>
  <verify>
    <automated>npx vitest run src/components/app/AppContent.test.tsx && npm run typecheck && npm run build</automated>
  </verify>
  <acceptance_criteria>Existing sessions can be opened repeatedly without flicker or duplicate ghost tabs, closing a tab leaves the app in a valid next state, and add-tab no longer traps the user in a broken shell path.</acceptance_criteria>
  <done>The live shell uses one stable tab controller for route, session selection, add-tab, and close behavior, closing the blocker UAT gap for session-tab lifecycle.</done>
</task>

<task type="auto">
  <name>Task 2: Make dual-pane layout valid, explicit, and independent per pane</name>
  <files>src/contexts/LayoutContext.tsx, src/hooks/useHomePreferences.ts, src/types/home.ts, src/components/app/AppContent.tsx, src/components/app/view/LayoutSwitcher.tsx, src/components/app/view/PaneDropZone.tsx, src/components/main-content/view/MainContent.tsx, src/components/main-content/view/subcomponents/MainContentHeader.tsx, src/contexts/LayoutContext.test.tsx</files>
  <read_first>
    - .planning/phases/01-foundation/01-UAT.md
    - .planning/phases/01-foundation/01-UI-SPEC.md
    - src/contexts/LayoutContext.tsx
    - src/components/app/view/LayoutSwitcher.tsx
    - src/components/app/view/PaneDropZone.tsx
    - src/components/main-content/view/MainContent.tsx
  </read_first>
  <action>Wire `LayoutContext.tsx` into the live shell so switching between `single` and `dual` changes actual rendered pane structure instead of only mutating stored preferences. Enforce the UI-spec contract: entering dual mode requires a valid primary session context, the secondary pane must always receive an explicit `sessionId + projectName + tabId` assignment, and returning to single mode must clear secondary-pane state. Store or derive content-tab state per pane so switching one pane between `chat/files/shell/git/tasks/preview` does not mutate the other pane. `LayoutSwitcher.tsx`, `PaneDropZone.tsx`, and the new `Open in New Pane` command must all call the same validated pane-assignment path, and corrupted persisted pane descriptors must fall back safely rather than rendering blank panes.</action>
  <verify>
    <automated>npx vitest run src/contexts/LayoutContext.test.tsx src/components/app/AppContent.test.tsx && npm run typecheck && npm run build</automated>
  </verify>
  <acceptance_criteria>Dual mode can be entered and exited repeatedly, the secondary pane always shows an explicitly assigned session, switching one pane's content tab does not mutate the other pane, and single-mode re-entry clears secondary-pane state cleanly.</acceptance_criteria>
  <done>The blocker dual-pane UAT gap is closed because the shell now renders real two-pane behavior with validated state transitions.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Re-run the diagnosed Phase 01 shell UAT scenarios</name>
  <files>N/A - human verification checkpoint</files>
  <read_first>
    - .planning/phases/01-foundation/01-UAT.md
    - .planning/phases/01-foundation/01-UI-SPEC.md
  </read_first>
  <action>After the automated regression checks pass, run the app and verify the exact shell behaviors that previously failed in UAT: open an existing session from landing or sidebar, create a new session, return Home, close tabs in different orders, and enter/exit dual pane using both toolbar and tab-level actions. Record any remaining mismatch against the diagnosed UAT wording instead of rewriting the expected behavior.</action>
  <verify>
    <automated>npm run typecheck && npm run build</automated>
  </verify>
  <acceptance_criteria>The executor can demonstrate that the original blocker reports are no longer reproducible, or else can point to one narrowed remaining mismatch with exact reproduction steps.</acceptance_criteria>
  <done>The Phase 01 shell blockers are either confirmed fixed by the user or reduced to a precisely documented follow-up instead of a vague broken-state report.</done>
  <what-built>Live shell-tab wiring, stable tab lifecycle, and functional single/dual-pane layout</what-built>
  <how-to-verify>
    1. Launch from the root route, open an existing session, then open a second session and confirm the shell tab strip remains stable.
    2. Click Home, confirm landing returns without stale shell tabs, then reopen an existing session and confirm it becomes a stable shell tab instead of flickering.
    3. Close the active tab, then close the remaining session tab, and confirm the route and visible state update cleanly each time.
    4. Enter dual-pane mode from the layout switcher, then use the tab context menu to open a different session in the second pane.
    5. Change pane content tabs independently, return to single mode, and confirm the secondary pane assignment is cleared.
  </how-to-verify>
  <resume-signal>Type "approved" or describe the remaining mismatch exactly.</resume-signal>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                     | Description                                                                |
| -------------------------------------------- | -------------------------------------------------------------------------- |
| route/session intents -> shell tab state     | Multiple entry points can compete to own the active session/tab selection  |
| persisted pane descriptors -> rendered panes | Invalid pane state can render blank or cross-wired panes if not normalized |

## STRIDE Threat Register

| Threat ID  | Category | Component                                  | Disposition | Mitigation Plan                                                                                                                                  |
| ---------- | -------- | ------------------------------------------ | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------ |
| T-01-07-01 | T        | src/hooks/useAppTabs.ts                    | mitigate    | Centralize all open/select/close transitions so route and tab updates happen atomically and stale entries are removed during hydration           |
| T-01-07-02 | D        | src/contexts/LayoutContext.tsx             | mitigate    | Reject invalid pane assignments, require explicit secondary session metadata, and fall back to single mode when dual state is incomplete         |
| T-01-07-03 | E        | src/components/app/view/TabContextMenu.tsx | mitigate    | Constrain pane actions to sessions that are currently addressable by the shell so users cannot assign missing sessions into secondary pane state |

</threat_model>

<verification>
Run the focused shell/layout Vitest suites, `npm run typecheck`, and `npm run build` before the human verification checkpoint.
</verification>

<success_criteria>
The blocker UAT gaps for tab flicker, inability to open existing sessions, broken close behavior, and broken dual-pane behavior are closed through live-shell integration rather than disconnected component scaffolding.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-foundation-07-SUMMARY.md`
</output>
