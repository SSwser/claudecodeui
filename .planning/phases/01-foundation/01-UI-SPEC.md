---
phase: 01
slug: foundation
status: approved
shadcn_initialized: false
preset: none
created: 2026-04-11
---

# Phase 01 - UI Design Contract

> Visual and interaction contract for frontend phases. Generated by gsd-ui-researcher, verified by gsd-ui-checker.

---

## Stack Assumptions (No RESEARCH.md)

- Frontend stack remains React 18 + Vite + React Router.
- Styling remains Tailwind CSS + semantic CSS variables from src/index.css.
- Existing local UI primitives in src/shared/view/ui and src/components/ui are reused; no new parallel design system is introduced in this phase.
- Icon system remains lucide-react for generic UI and provider logo components for branded providers.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none (keep existing tokenized Tailwind + local primitives) |
| Preset | not applicable |
| Component library | local shadcn-style wrappers and shared primitives (no new init in Phase 1) |
| Icon library | lucide-react + existing provider logos |
| Font | system stack from src/index.css |

---

## Spacing Scale

Declared values (must be multiples of 4):

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Icon gaps, compact inline spacing |
| sm | 8px | Dense controls, list row inner spacing |
| md | 16px | Default block spacing and card inner padding |
| lg | 24px | Section spacing, panel padding |
| xl | 32px | Major card/group separation |
| 2xl | 48px | Page section breaks |
| 3xl | 64px | Top-level page rhythm |

Exceptions: minimum touch target 44px for icon-only buttons (tab close, layout toggles, quick actions).

---

## Typography

| Role | Size | Weight | Line Height |
|------|------|--------|-------------|
| Body | 16px | 400 | 1.5 |
| Label | 14px | 600 | 1.4 |
| Heading | 20px | 600 | 1.25 |
| Display | 28px | 600 | 1.2 |

---

## Color

| Role | Value | Usage |
|------|-------|-------|
| Dominant (60%) | hsl(var(--background)) | App canvas, landing base, shell base |
| Secondary (30%) | hsl(var(--card)) / hsl(var(--secondary)) | Cards, filter surfaces, tab strip container, sidebar container |
| Accent (10%) | hsl(var(--primary)) | Primary CTA, active tab indicator, selected chips, focus ring |
| Destructive | hsl(var(--destructive)) | Delete workspace/session confirmations only |

Accent reserved for: primary CTA buttons, active shell tab/highlight, focused input ring, selected filter control, progress/active status emphasis. Accent is not used for passive card backgrounds.

---

## Copywriting Contract

| Element | Copy |
|---------|------|
| Primary CTA | 新建会话 |
| Empty state heading | 还没有可继续的会话 |
| Empty state body | 先创建工作区或新建会话，系统会在这里显示最近活动。 |
| Error state | 无法加载会话数据。请刷新列表；如果仍失败，检查项目路径与连接状态。 |
| Destructive confirmation | 删除工作区: 此操作会移除工作区记录，若包含 worktree 可选择同时删除目录；删除会话: 会话将永久删除且不可恢复。 |

---

## UAT-Driven Adjustment Contract (2026-04-11)

1. Landing and login routing contract
- Auth gate remains first: unauthenticated users see login/setup first.
- After successful auth, root route must resolve to landing state when no explicit session route is present.
- Landing state is a chrome-light state: no shell tab strip, no persistent desktop sidebar.

2. Sidebar visibility on landing
- Desktop sidebar is hidden when root view mode is landing.
- Mobile overlay nav remains available via menu button only; it is not open by default on landing.
- Sidebar appears only after entering project/session context.

3. Tab flicker and lifecycle contract
- Shell tab strip mounts only in shell state, never in landing state.
- Home tab behavior is deterministic: selecting Home returns to landing and removes transient empty-shell visual residue.
- Add-tab behavior is explicit: if project context exists, create new session draft; if no project context, route to landing create flow.
- Tab close must always update active tab and route in one transition; no intermediate flashing state.

4. Dual-pane functional contract
- Entering dual mode requires a valid primary session context.
- Secondary pane always receives explicit session assignment (sessionId + projectName + tabId).
- Returning to single mode always clears secondary pane state.
- Pane content tabs are independent, and switching one pane must not mutate the other pane active tab.

5. Workspace flow alignment contract
- Wizard step 2 fields are mode-gated and validation-gated:
  - logical mode requires workspace path; optional github clone fields only when URL exists.
  - worktree mode requires workspace path + source path + branch name.
- Review step mirrors submitted payload exactly (no hidden defaults that change mode intent).
- All validation copy must explain next action, not only report missing field.

6. Visual polish baseline for Phase 1
- Landing sections (hero, filters, favorites, recent list) share consistent corner radius tiers (12/16/24) and single shadow depth scale.
- Stateful controls (tabs, filter chips, favorite actions) use one interaction language: hover, focus-visible, active, disabled.
- Contrast baseline: body text and key metadata must meet WCAG AA against both light and dark theme surfaces.

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | none added in this phase | not required |
| third-party | none | not applicable |

---

## Checker Sign-Off

- [x] Dimension 1 Copywriting: PASS
- [x] Dimension 2 Visuals: FLAG (non-blocking)
- [x] Dimension 3 Color: PASS
- [x] Dimension 4 Typography: PASS
- [x] Dimension 5 Spacing: PASS
- [x] Dimension 6 Registry Safety: PASS

**Approval:** VERIFIED (2026-04-11)
**Notes:** Visual dimension has two non-blocking recommendations: define a primary focal point and add icon-only a11y fallback conventions.
