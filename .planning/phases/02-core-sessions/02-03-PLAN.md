---
phase: 02-core-sessions
plan: 03
type: execute
wave: 2
depends_on: [02-01, 02-02]
files_modified:
  - src/components/sidebar/view/Sidebar.tsx
  - src/components/sidebar/view/subcomponents/SidebarContent.tsx
  - src/components/sidebar/view/subcomponents/GlobalRecentsSection.tsx
  - src/components/sidebar/view/subcomponents/SidebarProjectListSection.tsx
  - src/components/sidebar/view/subcomponents/WorkspaceIndicator.tsx
  - src/components/sidebar/hooks/useSidebarController.ts
autonomous: true
requirements: [NAV-01, NAV-02, NAV-03, NAV-05]

must_haves:
  truths:
    - 'Sidebar top section shows last 10 global recent sessions'
    - 'Clicking a recent session in Sidebar navigates directly to chat interface'
    - 'Sidebar bottom section shows project list with name and active status dot'
    - 'Clicking a project in Sidebar opens Project Inbox in MainContent'
    - 'Sessions are NOT listed inside the Sidebar — they live in Project Inbox'
    - 'When multi-workspace enabled, Sidebar bottom shows passive workspace name indicator'
  artifacts:
    - path: 'src/components/sidebar/view/subcomponents/GlobalRecentsSection.tsx'
      provides: 'Global recent sessions section (top of sidebar)'
    - path: 'src/components/sidebar/view/subcomponents/SidebarProjectListSection.tsx'
      provides: 'Simplified project list (name + active dot only)'
    - path: 'src/components/sidebar/view/subcomponents/WorkspaceIndicator.tsx'
      provides: 'Passive workspace name display at sidebar bottom'
  key_links:
    - from: 'GlobalRecentsSection.tsx'
      to: '/api/projects/:id/sessions'
      via: 'Fetches recent sessions across all projects'
      pattern: 'fetch.*sessions.*recent'
    - from: 'SidebarProjectListSection.tsx'
      to: '/api/projects'
      via: 'Fetches project list for sidebar display'
      pattern: 'fetch.*projects'
---

<objective>
Complete Sidebar restructure: two-section layout with global recents (top) and project list (bottom).

Purpose: Per D-08/D-09, sessions move out of Sidebar entirely. The Sidebar becomes a lightweight navigation surface — global recents for quick access, project list for drill-down into Project Inbox.
Output: Restructured Sidebar with GlobalRecentsSection, SidebarProjectListSection, and optional WorkspaceIndicator.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-core-sessions/02-CONTEXT.md
@.planning/phases/02-core-sessions/02-RESEARCH.md
@.planning/phases/02-core-sessions/02-01-SUMMARY.md
@src/components/sidebar/view/Sidebar.tsx
@src/components/sidebar/view/subcomponents/SidebarContent.tsx
@src/components/sidebar/hooks/useSidebarController.ts
@src/components/home/view/RecentSessionsList.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create GlobalRecentsSection and SidebarProjectListSection components</name>
  <files>src/components/sidebar/view/subcomponents/GlobalRecentsSection.tsx, src/components/sidebar/view/subcomponents/SidebarProjectListSection.tsx, src/components/sidebar/view/subcomponents/WorkspaceIndicator.tsx</files>
  <action>
  1. Create `GlobalRecentsSection.tsx` (D-09 top section):
  - Fetch last 10 sessions across all projects via new API endpoint or derive from existing project data.
  - Each item shows: session name/title, project name (subtle), provider icon, time ago.
  - Clicking a recent session calls `onSessionSelect(sessionId)` → navigates to chat.
  - Favorite sessions appear pinned at top of the recents list (D-09: favorites live in Project Inbox but display in recents).
  - Reuse styling patterns from existing `RecentSessionsList.tsx` in Phase 1's home components.
  - Use semantic Tailwind tokens: `bg-sidebar`, `text-sidebar-foreground`, `hover:bg-sidebar-accent`.
  - Section header: translated label via `useTranslation('sidebar')` key `sidebar:recents`.

2. Create `SidebarProjectListSection.tsx` (D-09 bottom section):

- Fetch project list from `GET /api/projects?sort=recent` (from Plan 02 API).
- Each project shows: name + colored dot indicator (green if has active sessions, gray otherwise).
- **No expanding session list** — this is the key change from current Sidebar.
- Clicking a project: navigates to `/project/:id` route (opens Project Inbox in MainContent per D-10).
- Right-click context menu: rename, delete (soft-delete per D-22).
- "New Project" button at section bottom → opens project creation wizard.
- Use `lucide-react` icons: `FolderOpen` for projects, `Plus` for new project.

3. Create `WorkspaceIndicator.tsx` (D-15 passive indicator):

- Only rendered when current project has multi_workspace_enabled=true.
- Shows current workspace name as a subtle chip/badge at the bottom of sidebar.
- Read-only — the active workspace switcher is in MainContent header (Plan 04).
- Style: small text, `text-muted-foreground`, similar to update notification chip pattern.
  </action>
  <verify>
  <automated>npx tsc --noEmit src/components/sidebar/view/subcomponents/GlobalRecentsSection.tsx src/components/sidebar/view/subcomponents/SidebarProjectListSection.tsx src/components/sidebar/view/subcomponents/WorkspaceIndicator.tsx</automated>
  </verify>
  <done>Three new sidebar sections compile without errors. GlobalRecentsSection shows recent sessions, SidebarProjectListSection shows projects without session nesting, WorkspaceIndicator shows workspace name passively.</done>
  </task>

<task type="auto">
  <name>Task 2: Refactor SidebarContent and useSidebarController for new layout</name>
  <files>src/components/sidebar/view/subcomponents/SidebarContent.tsx, src/components/sidebar/hooks/useSidebarController.ts, src/components/sidebar/view/Sidebar.tsx</files>
  <action>
  1. Refactor `SidebarContent.tsx`:
  - Replace current `SidebarProjectList` (which nests sessions) with new two-section layout:
    ```
    <div className="flex flex-col h-full">
      <GlobalRecentsSection ... />
      <div className="border-t border-sidebar-border" />
      <SidebarProjectListSection ... />
      <WorkspaceIndicator ... />  <!-- conditional -->
    </div>
    ```
  - Remove imports and usage of `SidebarProjectSessions`, `SidebarSessionItem` from the sidebar tree.
  - Keep existing search filter functionality at the top of SidebarContent (filters both recents and projects per search term).

2. Simplify `useSidebarController.ts`:

- Remove session-specific logic that was previously in the Sidebar:
  - Remove `getProjectSessions`, `loadMoreSessions`, `loadingSessions`, `initialSessionsLoaded`
  - Remove `handleSessionClick` (sessions are now in Project Inbox, not Sidebar)
  - Remove `showDeleteSessionConfirmation`, `confirmDeleteSession`, `sessionDeleteConfirmation`
- Keep: project selection logic, sidebar collapse/expand, search filter, project CRUD (rename, delete)
- Add: `recentSessions` state (fetched from API or derived), project list fetch from new API.
- The hook should now be significantly simpler since all session management moved to Project Inbox.

3. Update `Sidebar.tsx`:

- Remove session-related props from `SidebarProps` type:
  - Remove `selectedSession`, `onSessionSelect`, `onNewSession`, `onSessionDelete`
- Add: `onProjectSelect(projectId: number)` for navigating to Project Inbox
- Pass simplified props to SidebarContent.
- Keep: project-level operations, sidebar visibility, search, settings.

**Preserving backward compatibility:** Since this changes the Sidebar API, update the parent component that renders `<Sidebar .../>` to pass the new props. The parent is in `AppContent` or equivalent shell component.
</action>
<verify>
<automated>npx tsc --noEmit src/components/sidebar/view/Sidebar.tsx</automated>
</verify>
<done>Sidebar renders two-section layout. Sessions no longer appear inside Sidebar. Project clicks navigate to Project Inbox route. useSidebarController simplified.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description                                   |
| -------- | --------------------------------------------- |
| API→UI   | Session/project data from API displayed in UI |

## STRIDE Threat Register

| Threat ID | Category          | Component             | Disposition | Mitigation Plan                                              |
| --------- | ----------------- | --------------------- | ----------- | ------------------------------------------------------------ |
| T-02-08   | Info Disclosure   | GlobalRecentsSection  | accept      | Displays user's own session data; single-user app            |
| T-02-09   | Denial of Service | Recent sessions fetch | mitigate    | Limit to 10 items in API query; do not fetch unbounded lists |

</threat_model>

<verification>
- Sidebar renders with two visible sections (recents + projects)
- Clicking a project navigates to project inbox route
- Clicking a recent session navigates to session chat
- No session list appears inside the sidebar
- Multi-workspace indicator shows when applicable
</verification>

<success_criteria>

- Sidebar top shows global recent sessions (max 10)
- Sidebar bottom shows project list with active status dots
- Sessions are completely absent from Sidebar
- Workspace indicator appears for multi-workspace projects
- All existing sidebar functionality (collapse, search, settings) preserved
  </success_criteria>

<output>
After completion, create `.planning/phases/02-core-sessions/02-03-SUMMARY.md`
</output>
