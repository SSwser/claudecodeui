---
phase: 02-core-sessions
plan: 04
type: execute
wave: 2
depends_on: [02-01, 02-02]
files_modified:
  - src/components/project-inbox/view/ProjectInbox.tsx
  - src/components/project-inbox/view/ProjectInboxHeader.tsx
  - src/components/project-inbox/view/SessionCard.tsx
  - src/components/project-inbox/hooks/useProjectInbox.ts
  - src/components/project-inbox/types/types.ts
  - src/components/project-wizard/view/ProjectWizard.tsx
  - src/components/project-wizard/view/WizardStepBasics.tsx
  - src/components/project-wizard/view/WizardStepDirectory.tsx
  - src/components/project-wizard/view/WizardStepWorkspace.tsx
autonomous: true
requirements: [NAV-04, PROJ-01, PROJ-04, PROJ-06, SESS-01, SESS-06, SESS-07]

must_haves:
  truths:
    - 'Project Inbox renders in MainContent when a project is selected'
    - 'Session cards show name, timestamp, status badge, and provider icon'
    - 'Hover/long-press on session card shows last message preview'
    - '3-step project creation wizard works (name → directory → workspace toggle)'
    - 'Session list in Project Inbox supports search by name'
    - 'Clicking a session in Project Inbox opens it in a Tab'
  artifacts:
    - path: 'src/components/project-inbox/view/ProjectInbox.tsx'
      provides: 'Main Inbox view with session list, search, and CRUD'
    - path: 'src/components/project-inbox/view/SessionCard.tsx'
      provides: 'Session card with status badge, provider icon, timestamp'
    - path: 'src/components/project-inbox/hooks/useProjectInbox.ts'
      provides: 'Data fetching and state management for Inbox'
    - path: 'src/components/project-wizard/view/ProjectWizard.tsx'
      provides: '3-step project creation wizard component'
  key_links:
    - from: 'useProjectInbox.ts'
      to: '/api/projects/:id/sessions'
      via: 'Fetches sessions for project'
      pattern: 'fetch.*projects.*sessions'
    - from: 'ProjectWizard.tsx'
      to: '/api/projects'
      via: 'POST to create project'
      pattern: 'fetch.*projects.*POST'
---

<objective>
Project Inbox (MainContent view) and 3-step Project Creation Wizard.

Purpose: Per D-10/D-11, the Project Inbox is the primary session management surface — replaces the old sidebar session list. The wizard implements D-17/D-18 for manual project setup.
Output: ProjectInbox component with session cards, search, CRUD; ProjectWizard with 3 steps.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-core-sessions/02-CONTEXT.md
@.planning/phases/02-core-sessions/02-RESEARCH.md
@.planning/phases/02-core-sessions/02-01-SUMMARY.md
@.planning/phases/02-core-sessions/02-02-SUMMARY.md
@src/types/project.ts
@src/types/session.ts
@src/types/app.ts
@src/components/home/view/LandingPage.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Project Inbox component with session cards</name>
  <files>src/components/project-inbox/view/ProjectInbox.tsx, src/components/project-inbox/view/ProjectInboxHeader.tsx, src/components/project-inbox/view/SessionCard.tsx, src/components/project-inbox/hooks/useProjectInbox.ts, src/components/project-inbox/types/types.ts</files>
  <action>
  1. Create `src/components/project-inbox/types/types.ts`:
  - Define `ProjectInboxProps { projectId: number }`
  - Define `SessionCardProps { session: SessionState; onSelect; onFreeze; onArchive; onDelete }`

2. Create `useProjectInbox.ts` hook:

- Takes `projectId` as input
- Fetches sessions from `GET /api/projects/:id/sessions` (from Plan 02)
- Manages filter state: `statusFilter` (all/active/frozen/archived), `searchQuery`
- Instant title filtering (client-side LIKE on already-fetched sessions per D-21)
- Debounced full-text search sends `q=` param to backend (triggers FTS5 in Plan 07)
- Listens for WebSocket `session_state_changed` messages to update session list in real-time
- Provides: `sessions`, `isLoading`, `searchQuery`, `setSearchQuery`, `statusFilter`, `setStatusFilter`, `sortOrder`, `setSortOrder`
- Sort: by last activity (default), by name, by created date (per D-06)

3. Create `ProjectInboxHeader.tsx`:

- Project name as title
- Search input field (instant title filter + debounced content search per D-19/D-21)
- Status filter tabs/chips: All | Active | Frozen | Archived
- Sort dropdown: Recent | Name | Created
- When project has multi_workspace_enabled: Workspace switcher dropdown (D-15 action location)
- "New Session" button (D-01: creates session via API)

4. Create `SessionCard.tsx` (D-11):

- Displays: session name/title, last message timestamp (relative time), status badge, AI provider icon
- Status badge uses semantic colors: Active=green, Frozen=blue/snowflake, Archived=gray
- Provider icon: reuse existing `SessionProviderLogo` from `src/components/llm-logo-provider/`
- Hover (desktop): show last message preview in tooltip/popover
- Long-press (mobile): same preview behavior
- When multi-workspace enabled (D-16): show worktree label on card
- Click: calls `onSelect(session)` → opens session tab
- Context menu (right-click): Freeze, Archive, Delete, Rename options

5. Create `ProjectInbox.tsx`:

- Composes: `ProjectInboxHeader` + scrollable `SessionCard` list + `EmptyState`
- EmptyState: when no sessions, show message with "Create your first session" CTA
- Uses `useProjectInbox` hook for data
- Layout: full-width within MainContent area, responsive (stack on mobile)
- Style: Tailwind semantic tokens, consistent with existing design system

Per D-10: "简化 Inbox 风格：会话列表、状态 Badge、时间戳、AI 提供商图标、搜索、CRUD"
</action>
<verify>
<automated>npx tsc --noEmit src/components/project-inbox/view/ProjectInbox.tsx src/components/project-inbox/hooks/useProjectInbox.ts</automated>
</verify>
<done>ProjectInbox renders session cards with status badges, search, filter, and sort. Session cards show all required info (name, timestamp, badge, provider icon). Hover preview works.</done>
</task>

<task type="auto">
  <name>Task 2: Create 3-step Project Creation Wizard</name>
  <files>src/components/project-wizard/view/ProjectWizard.tsx, src/components/project-wizard/view/WizardStepBasics.tsx, src/components/project-wizard/view/WizardStepDirectory.tsx, src/components/project-wizard/view/WizardStepWorkspace.tsx</files>
  <action>
  1. Create `ProjectWizard.tsx` (D-17):
  - 3-step wizard with step indicator (stepper dots or numbered steps)
  - Step 1: Project basics (name input)
  - Step 2: Directory selection (local folder path or git repo URL)
  - Step 3: Optional workspace management toggle
  - Navigation: Back / Next / Create buttons
  - On create: POST to `/api/projects` with collected data
  - After creation: close wizard, navigate to the new project's Inbox
  - Rendered as a modal/dialog overlay (reuse existing Dialog primitive from `src/shared/view/ui/`)

2. Create `WizardStepBasics.tsx`:

- Project name input (required, 3-100 chars)
- Validation: show error if name is empty or too short
- Auto-focus name input on mount

3. Create `WizardStepDirectory.tsx`:

- Directory path input with "Browse" button (if Electron-like file dialog available, otherwise text input)
- Alternative: git repo URL input (reuses existing clone logic from Phase 1's workspace creation)
- Path validation: show warning if path doesn't exist (can still proceed for git clone)
- Per D-18: fully manual import — user specifies the directory

4. Create `WizardStepWorkspace.tsx` (D-13):

- Toggle switch: "Enable multi-workspace management"
- Description text explaining what this does and that it's irreversible
- Default: OFF
- When ON: shows additional info about git worktree-based workspaces
- Warning badge: "This cannot be undone" per D-13

Use `useTranslation('projects')` for all labels. Create translation keys as needed.
</action>
<verify>
<automated>npx tsc --noEmit src/components/project-wizard/view/ProjectWizard.tsx</automated>
</verify>
<done>3-step wizard renders with validation. Creates project via API. Navigates to new project Inbox after creation.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary        | Description                     |
| --------------- | ------------------------------- |
| User input→API  | Wizard form data sent to API    |
| API response→UI | Session data displayed in cards |

## STRIDE Threat Register

| Threat ID | Category  | Component                 | Disposition | Mitigation Plan                                                              |
| --------- | --------- | ------------------------- | ----------- | ---------------------------------------------------------------------------- |
| T-02-10   | Tampering | Wizard inputs             | mitigate    | Validate name length (3-100), path format client-side; server also validates |
| T-02-11   | Spoofing  | Session card context menu | accept      | Actions route through authenticated API; single-user app                     |

</threat_model>

<verification>
- ProjectInbox renders session list with correct status badges
- Search filters sessions by title instantly
- ProjectWizard completes 3-step flow and creates project
- Session card click opens session in a tab
- Empty state shows when no sessions exist
</verification>

<success_criteria>

- Project Inbox is the primary session management surface in MainContent
- Session cards display all required info per D-11
- 3-step wizard creates projects per D-17
- Multi-workspace toggle works with irreversibility warning per D-13
- WebSocket updates refresh session list in real-time
  </success_criteria>

<output>
After completion, create `.planning/phases/02-core-sessions/02-04-SUMMARY.md`
</output>
