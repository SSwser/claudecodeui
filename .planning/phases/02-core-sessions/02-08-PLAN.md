---
phase: 02-core-sessions
plan: 08
type: execute
wave: 4
depends_on: [02-03, 02-04, 02-05, 02-06, 02-07]
files_modified:
  - src/App.tsx
  - src/components/layout/view/MainContent.tsx
  - src/components/home/view/GlobalRecentsSection.tsx
  - src/contexts/WebSocketContext.tsx
  - server/app.js
autonomous: false
requirements: [NAV-01, NAV-02, NAV-03, NAV-05, PROJ-02, PROJ-03, PROJ-05]

must_haves:
  truths:
    - 'Clicking a project in sidebar navigates to Project Inbox in MainContent'
    - 'Home/default view shows global recent sessions across all projects'
    - 'Landing page is removed — replaced by sidebar + welcome placeholder (D-27)'
    - 'All new routes and API endpoints are mounted in app.js'
    - 'WebSocket context handles session_state_changed messages'
    - 'Session tabs show status indicators and lifecycle actions'
  artifacts:
    - path: 'src/App.tsx'
      provides: 'Updated routing with /project/:id and home view'
    - path: 'src/components/layout/view/MainContent.tsx'
      provides: 'Content router dispatching to Inbox, Session, or Home'
    - path: 'src/components/home/view/GlobalRecentsSection.tsx'
      provides: 'Recent sessions from all projects on home view'
  key_links:
    - from: 'SidebarProjectItem (Plan 03)'
      to: 'App.tsx router'
      via: "navigate('/project/:id') on project click"
      pattern: 'navigate.*project'
    - from: 'WebSocketContext.tsx'
      to: 'useSessionLifecycle.ts'
      via: 'session_state_changed event dispatch'
      pattern: 'session_state_changed'
    - from: 'server/app.js'
      to: 'sessionLifecycle.js + search.js routes'
      via: 'Express route mounting'
      pattern: "app\\.use.*sessions|search"
---

<objective>
Wire all Phase 2 components together: routing, WebSocket integration, API mounting, and end-to-end navigation flow.

Purpose: Connect sidebar → Project Inbox → session lifecycle → search into a cohesive user experience. Replace Landing Page with sidebar + placeholder per D-27.
Output: Working navigation flow from sidebar project click through to session management.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-core-sessions/02-CONTEXT.md
@.planning/phases/02-core-sessions/02-03-SUMMARY.md
@.planning/phases/02-core-sessions/02-04-SUMMARY.md
@.planning/phases/02-core-sessions/02-05-SUMMARY.md
@.planning/phases/02-core-sessions/02-06-SUMMARY.md
@.planning/phases/02-core-sessions/02-07-SUMMARY.md
@src/App.tsx
@src/contexts/WebSocketContext.tsx
@server/app.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire routing, WebSocket, and API mounting</name>
  <files>src/App.tsx, src/contexts/WebSocketContext.tsx, server/app.js, src/components/layout/view/MainContent.tsx</files>
  <action>
  1. Modify `server/app.js`:
  - Mount new route modules:
    ```js
    const sessionLifecycleRoutes = require('./routes/sessionLifecycle');
    const searchRoutes = require('./routes/search');
    app.use('/api/sessions', sessionLifecycleRoutes);
    app.use('/api/search', searchRoutes);
    ```
  - Ensure FTS5 migration runs during `initializeDatabase()` (should already be handled by migration system from Plan 07)

2. Modify `src/App.tsx`:

- Add new route: `/project/:projectId` → renders ProjectInbox component
- Keep existing `/session/:sessionId` route for direct session access
- Default route `/` → renders Home view (GlobalRecentsSection + welcome area)
- Remove or replace LandingPage import per D-27 — Landing Page REMOVED
- Import ProjectInbox and pass projectId from route params

3. Modify `src/contexts/WebSocketContext.tsx`:

- Add handler for `session_state_changed` message type:
  ```ts
  case 'session_state_changed':
    // Dispatch custom event that useSessionLifecycle and useProjectInbox can listen to
    window.dispatchEvent(new CustomEvent('session-state-changed', {
      detail: { sessionId: msg.sessionId, status: msg.status, timestamp: msg.timestamp }
    }));
    break;
  ```
- Add handler for `project_updated` message type (from Plan 02):
  ```ts
  case 'project_updated':
    window.dispatchEvent(new CustomEvent('project-updated', {
      detail: { projectId: msg.projectId, action: msg.action }
    }));
    break;
  ```

4. Create/modify `src/components/layout/view/MainContent.tsx`:

- Acts as the content area router/dispatcher based on current route:
  - `/project/:projectId` → render `<ProjectInbox projectId={id} />`
  - `/session/:sessionId` → render existing session chat view
  - `/` → render `<HomeView />`
- HomeView shows welcome message when no project selected (replaces LandingPage per D-27)
- Responsive layout: full width with max-width constraint
  </action>
  <verify>
  <automated>npx tsc --noEmit src/App.tsx</automated>
  </verify>
  <done>Routes work: /project/:id shows Inbox, /session/:id shows chat, / shows home. API endpoints mounted. WebSocket handlers dispatch lifecycle events.</done>
  </task>

<task type="auto">
  <name>Task 2: Create GlobalRecentsSection and HomeView</name>
  <files>src/components/home/view/GlobalRecentsSection.tsx</files>
  <action>
  1. Create `src/components/home/view/GlobalRecentsSection.tsx` (D-27):
  - Replaces the removed LandingPage as the home/default content area
  - Fetches recent sessions across ALL projects via `GET /api/sessions/recent?limit=10`
    - If this endpoint doesn't exist yet: create a simple helper in the existing sessions route that queries session_state ORDER BY updated_at DESC LIMIT 10
  - Renders a list of recent SessionCards (reuse SessionCard from Plan 04)
  - Each card shows: session title, project name, provider, timestamp, status badge
  - Click opens session in tab
  - Section header: "Recent Sessions" with icon
  - Below recents: welcome area with quick actions:
    - "Create Project" button (opens ProjectWizard from Plan 04)
    - Brief onboarding text for new users (when no sessions exist)
  - Layout: centered content, max-width-4xl, with spacing matching existing design
  - Reuse patterns from existing `RecentSessionsList.tsx` in `src/components/home/`

2. Ensure smooth navigation flow end-to-end:

- Sidebar project click → `navigate('/project/${projectId}')` → ProjectInbox renders
- Sidebar recents click → `navigate('/session/${sessionId}')` → Chat view renders
- Project Inbox session click → open Tab → chat view renders
- Home "Create Project" → ProjectWizard modal → after creation → navigate to new project Inbox
  </action>
  <verify>
  <automated>npx tsc --noEmit src/components/home/view/GlobalRecentsSection.tsx</automated>
  </verify>
  <done>Home view shows global recent sessions. Navigation flow works end-to-end: sidebar → inbox → session. LandingPage replaced per D-27.</done>
  </task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete Phase 2 integration: sidebar navigation → Project Inbox → session lifecycle → search → routing</what-built>
  <how-to-verify>
    1. Start the app: `npm run dev`
    2. Check sidebar shows "Recents" section (3 pinned slots) and "Projects" section with project list
    3. Click a project in sidebar → MainContent shows Project Inbox with session list
    4. In Project Inbox: verify session cards show name, timestamp, status badge, provider icon
    5. Hover a session card → see last message preview
    6. Type in search → title matches appear instantly; after brief pause, content matches appear
    7. Click "Freeze" on an active session → confirm dialog → session shows frozen badge, input disabled
    8. Click "Resume" on frozen session → session becomes active again
    9. Right-click a session → context menu shows Freeze/Archive/Delete options
    10. Navigate to home (click logo/home) → see Global Recent Sessions across all projects
    11. Click "Create Project" → 3-step wizard appears → create a test project → navigates to its Inbox
    12. Verify no console errors throughout
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                | Description                                     |
| ----------------------- | ----------------------------------------------- |
| Route params→Components | projectId/sessionId from URL used to fetch data |

## STRIDE Threat Register

| Threat ID | Category               | Component            | Disposition | Mitigation Plan                                                                 |
| --------- | ---------------------- | -------------------- | ----------- | ------------------------------------------------------------------------------- |
| T-02-17   | Tampering              | Route projectId      | mitigate    | Validate projectId is integer in route handler; return 404 for unknown projects |
| T-02-18   | Information Disclosure | GlobalRecentsSection | accept      | Single-user local app; all sessions belong to the user                          |

</threat_model>

<verification>
- Navigation: sidebar project click → Project Inbox renders correctly
- Navigation: session click in Inbox → session chat opens in tab
- Navigation: home route shows recent sessions across all projects
- WebSocket: session_state_changed events update UI in real-time
- API: all new endpoints (/sessions/*, /search) are accessible
- LandingPage is removed per D-27
- End-to-end flow: create project → add session → freeze → resume → search
</verification>

<success_criteria>

- Complete navigation flow works end-to-end with no dead ends
- All API routes mounted and functional
- WebSocket events drive real-time updates across all surfaces
- LandingPage replaced by sidebar + GlobalRecentsSection per D-27
- Human verification confirms visual correctness and interaction quality
  </success_criteria>

<output>
After completion, create `.planning/phases/02-core-sessions/02-08-SUMMARY.md`
</output>
