---
phase: 04-ui-migration
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/index.css
  - tailwind.config.js
  - src/shared/view/ui/Button.tsx
  - src/shared/view/ui/Badge.tsx
  - src/shared/view/ui/Input.tsx
autonomous: true
requirements:
  - UI-01
  - UI-02
  - UI-04
must_haves:
  truths:
    - "CSS variables cover surfaces, borders, typography scale, and spacing — no hardcoded values in foundational primitives"
    - "Button, Badge, and Input exclusively use semantic token classes (bg-surface-*, text-foreground, border-border, etc.)"
    - "Dark-mode equivalents exist for all new tokens in .dark {} block"
    - "Tailwind config bridges all new CSS vars to Tailwind utility classes"
  artifacts:
    - path: "src/index.css"
      provides: "Expanded CSS variable set: surface, typography, sidebar, spacing tokens"
      contains: "--surface-1, --surface-2, --text-xs, --font-sans"
    - path: "tailwind.config.js"
      provides: "Tailwind semantic bridge for new tokens"
      contains: "surface, typography extensions"
    - path: "src/shared/view/ui/Button.tsx"
      provides: "Token-clean Button with cva variants"
      min_lines: 35
    - path: "src/shared/view/ui/Badge.tsx"
      provides: "Token-clean Badge with cva variants"
      min_lines: 20
    - path: "src/shared/view/ui/Input.tsx"
      provides: "Token-clean Input referencing border-input, bg-background"
      min_lines: 15
  key_links:
    - from: "src/index.css"
      to: "tailwind.config.js"
      via: "hsl(var(--token)) bridge"
      pattern: "hsl\\(var\\(--"
    - from: "tailwind.config.js"
      to: "src/shared/view/ui/Button.tsx"
      via: "Tailwind class names referencing semantic tokens"
      pattern: "bg-primary|bg-secondary|text-foreground"
---

<objective>
Expand the CSS variable system beyond colors to cover themeable surfaces, borders, radius,
spacing, and typography. Update foundational shared primitives (Button, Badge, Input) to
exclusively use the new semantic tokens — eliminating any residual hardcoded values.

Purpose: Establishes the token foundation required by all subsequent Phase 4 migration work.
Output: Extended index.css, updated tailwind.config.js, and three token-clean primitives.
</objective>

<execution_context>
@$HOME/.copilot/get-shit-done/workflows/execute-plan.md
@$HOME/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-ui-migration/04-CONTEXT.md

@DESIGN.md
@src/index.css
@tailwind.config.js
@src/shared/view/ui/Button.tsx
@src/shared/view/ui/Badge.tsx
@src/shared/view/ui/Input.tsx
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Expand CSS variable system with surface, typography, and spacing tokens</name>
  <files>src/index.css, tailwind.config.js</files>
  <action>
Extend the existing `:root` block in `src/index.css` with new semantic token groups. Add
ONLY the groups below — do not touch or rename any existing tokens (colors, radius, nav, safe-area).

**New tokens to add in `:root` (per D-04, D-05, D-06):**

```css
/* Surface hierarchy tokens */
--surface-1: 0 0% 100%;            /* Page canvas (same as --background) */
--surface-2: 210 40% 98%;          /* Slightly elevated panel */
--surface-3: 210 40% 96.1%;        /* Card / popover surface */
--surface-elevated: 0 0% 100%;     /* Top-layer surface (modals) */

/* Sidebar / shell tokens */
--sidebar-bg: 222.2 84% 4.9%;      /* Sidebar background */
--sidebar-fg: 210 40% 98%;         /* Sidebar foreground text */
--sidebar-border: 217.2 32.6% 17.5%; /* Sidebar border */
--sidebar-accent: 217.2 91.2% 59.8%; /* Sidebar active/accent */

/* Typography scale tokens */
--text-xs: 0.75rem;
--text-sm: 0.875rem;
--text-base: 1rem;
--text-lg: 1.125rem;
--text-xl: 1.25rem;
--text-2xl: 1.5rem;

--font-weight-normal: 400;
--font-weight-medium: 500;
--font-weight-semibold: 600;
--font-weight-bold: 700;

/* Spacing scale (supplements Tailwind defaults) */
--spacing-1: 0.25rem;
--spacing-2: 0.5rem;
--spacing-3: 0.75rem;
--spacing-4: 1rem;
--spacing-6: 1.5rem;
--spacing-8: 2rem;
```

**Add matching `.dark` overrides:**
```css
/* Surface hierarchy (dark) */
--surface-1: 222.2 84% 4.9%;
--surface-2: 222.2 47.4% 8%;
--surface-3: 217.2 32.6% 12%;
--surface-elevated: 217.2 32.6% 14%;

/* Sidebar (dark) */
--sidebar-bg: 222.2 84% 4.9%;
--sidebar-fg: 210 40% 98%;
--sidebar-border: 217.2 32.6% 17.5%;
--sidebar-accent: 217.2 91.2% 59.8%;
```

**Update `tailwind.config.js`** — add these extensions inside `theme.extend`:
```js
colors: {
  // existing entries ...
  surface: {
    1: 'hsl(var(--surface-1))',
    2: 'hsl(var(--surface-2))',
    3: 'hsl(var(--surface-3))',
    elevated: 'hsl(var(--surface-elevated))',
  },
  sidebar: {
    DEFAULT: 'hsl(var(--sidebar-bg))',
    foreground: 'hsl(var(--sidebar-fg))',
    border: 'hsl(var(--sidebar-border))',
    accent: 'hsl(var(--sidebar-accent))',
  },
},
fontSize: {
  xs:   ['var(--text-xs)',   { lineHeight: '1rem' }],
  sm:   ['var(--text-sm)',   { lineHeight: '1.25rem' }],
  base: ['var(--text-base)', { lineHeight: '1.5rem' }],
  lg:   ['var(--text-lg)',   { lineHeight: '1.75rem' }],
  xl:   ['var(--text-xl)',   { lineHeight: '1.75rem' }],
  '2xl':['var(--text-2xl)', { lineHeight: '2rem' }],
},
fontWeight: {
  normal:    'var(--font-weight-normal)',
  medium:    'var(--font-weight-medium)',
  semibold:  'var(--font-weight-semibold)',
  bold:      'var(--font-weight-bold)',
},
```

Do NOT add spacing overrides to Tailwind (Tailwind's native spacing scale is sufficient;
the CSS vars are exposed for non-Tailwind custom property usage only).

**Do not** add a parallel token system (no separate tokens object, no CSS-in-JS). Stays
fully within Tailwind + CSS variable model (per D-06).
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>
    - `:root` in index.css contains all surface, sidebar, typography, and spacing tokens
    - `.dark` block has surface + sidebar overrides
    - tailwind.config.js extends `surface`, `sidebar`, `fontSize`, and `fontWeight`
    - `npm run build` passes with no new errors
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Update Button, Badge, and Input to use only semantic tokens</name>
  <files>src/shared/view/ui/Button.tsx, src/shared/view/ui/Badge.tsx, src/shared/view/ui/Input.tsx</files>
  <action>
Audit the three primitives and replace any hardcoded Tailwind color values with their
semantic equivalents (per D-02, D-04, D-06, D-13 – follow Figma integration rules in
docs/figma/INTEGRATION-RULES.md before writing classes).

**Button.tsx:** Keep cva + cn structure unchanged. Review each variant:
- Ensure `default` variant uses `bg-primary text-primary-foreground` (already correct, verify)
- Ensure `outline` variant uses `border-border bg-background hover:bg-accent` (not bg-gray-* or literal colors)
- Ensure `ghost` variant uses `hover:bg-accent hover:text-accent-foreground`
- Ensure `destructive` variant uses `bg-destructive text-destructive-foreground`
- Ensure `secondary` variant uses `bg-secondary text-secondary-foreground`
- Add `surface` variant: `bg-surface-2 text-foreground hover:bg-surface-3 border border-border`
- Add `link` variant: `text-primary underline-offset-4 hover:underline p-0 h-auto`
- Export both `Button` and `buttonVariants` (already done)

**Badge.tsx:** Keep cva + cn structure.
- `default` → `bg-primary text-primary-foreground`
- `secondary` → `bg-secondary text-secondary-foreground`
- `destructive` → `bg-destructive text-destructive-foreground`
- `outline` → `border border-border text-foreground`
- Add `success` → `bg-green-100 text-green-800 dark:bg-green-900 dark:text-green-300`

**Input.tsx:** Keep cn compose. Ensure:
- Base classes use `border-input bg-background text-foreground`
- Focus classes use `ring-ring focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ring`
- Placeholder uses `placeholder:text-muted-foreground`
- Disabled uses `disabled:cursor-not-allowed disabled:opacity-50`
- Remove any hardcoded color like `border-gray-*` or `bg-white` if present

All three files must contain only semantic tokens — no hardcoded hex, no raw Tailwind
color utilities like `bg-gray-100`, `text-gray-500`, `border-gray-200`.
  </action>
  <verify>
    <automated>npm run typecheck 2>&1 | tail -10</automated>
  </verify>
  <done>
    - Button has `surface` and `link` variants added; all existing variants use only semantic tokens
    - Badge has `success` variant added; all variants use semantic tokens
    - Input uses `border-input bg-background text-foreground ring-ring` with no hardcoded colors
    - `npm run typecheck` passes with no new errors
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Build pipeline | Tailwind CSS token names compiled into class hash — invalid tokens produce warning, not runtime failure |
| CSS custom properties | Set in :root and .dark — no user-controllable values, no XSS vector |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | tailwind.config.js | accept | Token config is a build artifact, not runtime-injectable |
| T-04-02 | Information Disclosure | CSS vars in devtools | accept | UI tokens carry no sensitive data |
| T-04-03 | Spoofing | Theme switching (.dark class) | accept | ThemeContext controls class toggle via localStorage — no privilege escalation |
</threat_model>

<verification>
After both tasks complete:
1. Run `npm run build` — must pass
2. Run `npm run typecheck` — must pass
3. Run `npm run lint` — no new errors
4. Visually inspect a Button in all variants (default, outline, ghost, destructive, surface) in both light and dark mode
</verification>

<success_criteria>
- `src/index.css` has surface, sidebar, typography, and spacing token groups in both `:root` and `.dark`
- `tailwind.config.js` exposes `surface.*`, `sidebar.*`, `fontSize.*`, and `fontWeight.*` semantic classes
- Button, Badge, Input contain zero hardcoded Tailwind color utilities
- `npm run build` and `npm run typecheck` pass without new errors
</success_criteria>

<output>
After completion, create `.planning/phases/04-ui-migration/04-01-SUMMARY.md`
</output>
