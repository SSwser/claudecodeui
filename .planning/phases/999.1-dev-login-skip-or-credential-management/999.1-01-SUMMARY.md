---
phase: 999.1-dev-login-skip-or-credential-management
plan: '01'
subsystem: auth
tags:
  - auth
  - jwt
  - websocket
  - dev-login
requires: []
provides:
  - unified auth bypass for platform and dev auto-login modes
  - automatic dev user creation for fresh local databases
  - websocket admission that mirrors the HTTP bypass rules
affects:
  - server/constants/config.js
  - server/middleware/auth.js
  - server/index.js
tech_stack:
  added: []
  patterns:
    - auth bypass decisions centralized in shouldSkipAuth()
    - synchronous websocket auth backed by startup-time user pre-creation
key_files:
  created: []
  modified:
    - server/constants/config.js
    - server/middleware/auth.js
    - server/index.js
key_decisions:
  - Kept a single shouldSkipAuth() helper so REST and WebSocket auth cannot drift.
  - Pre-created the dev user during server startup because verifyClient must stay synchronous.
patterns-established:
  - DEV_AUTO_LOGIN follows the same exact-string env flag semantics as platform mode.
  - Fresh local databases can enter authenticated dev flows without a manual registration step.
requirements-completed: []
duration: 25min
completed: 2026-04-11
---

# Phase 999.1 Plan 01 Summary

**Server auth now supports a development-only auto-login path that bypasses JWT checks, provisions a default dev user on empty databases, and admits WebSocket upgrades without a token.**

## Performance

- **Duration:** 25 min
- **Completed:** 2026-04-11T15:25:00Z
- **Tasks:** 2
- **Files modified:** 3

## Accomplishments

- Added `IS_DEV_AUTO_LOGIN` as a server-side config flag with explicit opt-in semantics.
- Unified platform mode and dev auto-login bypass decisions via `shouldSkipAuth()` in auth middleware.
- Guaranteed WebSocket auth can remain synchronous by creating a default `dev/dev` user during startup when needed.

## Task Commits

Atomic commits were not created in this editor session.

- Task 1: not committed
- Task 2: not committed
- Plan metadata: not committed

## Files Created/Modified

- `server/constants/config.js` - exports the development auto-login feature flag.
- `server/middleware/auth.js` - centralizes auth skipping and ensures a dev user exists for HTTP bypass flows.
- `server/index.js` - provisions the dev user at startup and lets WebSocket verifyClient honor the same bypass.

## Decisions Made

- Added `ensureDevUser()` in the auth layer even though startup pre-creation is the primary path, so HTTP bypass remains resilient if the database starts empty.
- Kept the bypass off unless the env var is exactly `'true'` to avoid accidental activation from truthy strings.

## Deviations from Plan

### Auto-fixed Issues

**1. Middleware needed an async safety net for fresh databases**

- **Found during:** Task 1
- **Issue:** The plan assumed startup pre-creation would always run before any bypassed HTTP request.
- **Fix:** Added `ensureDevUser()` inside `authenticateToken()` for the dev bypass path while leaving WebSocket auth synchronous.
- **Files modified:** `server/middleware/auth.js`
- **Verification:** `get_errors` reported no issues; build passed.

---

**Total deviations:** 1 auto-fixed
**Impact on plan:** Improved resilience without changing the intended external behavior.

## Issues Encountered

- None.

## User Setup Required

- Set `VITE_DEV_AUTO_LOGIN=true` in the local environment to enable the bypass.
- Remove the flag for normal OSS authentication behavior.

## Verification

- `get_errors` on the modified server files returned no errors.
- `npm run build` passed after the server changes landed.
- Source grep confirmed `IS_DEV_AUTO_LOGIN` and `shouldSkipAuth()` are present in the intended server files.

## Next Phase Readiness

- Backend is ready for local manual UAT without a JWT token.
- Frontend can now rely on the same env flag to enter the app directly.

---

_Phase: 999.1-dev-login-skip-or-credential-management_
_Completed: 2026-04-11_
