---
phase: 999.2-design-md
plan: '01'
type: execute
wave: 1
depends_on: []
files_modified:
  - src/App.tsx
  - src/components/auth/view/ProtectedRoute.tsx
  - src/components/design-system/view/VisualRegressionFixtures.tsx
  - tests/visual/baseline.spec.ts
  - tests/visual/screenshot.css
  - playwright.config.ts
autonomous: true
requirements:
  - BACKLOG-999.2-01
must_haves:
  truths:
    - 视觉截图来自稳定夹具页面，而不是当前登录态或偶然页面状态
    - home、auth、tabs、dialog、chat、settings、upgrade 至少各有一个命名截图入口
    - 后续计划可以在不改测试结构的前提下扩展或刷新截图
  artifacts:
    - src/components/design-system/view/VisualRegressionFixtures.tsx
    - tests/visual/baseline.spec.ts
    - tests/visual/screenshot.css
  key_links:
    - src/App.tsx routes the internal fixture view used by Playwright
    - tests/visual/baseline.spec.ts opens the fixture route and captures named states
---

<objective>
建立 Phase 999.2 的稳定视觉验证骨架，先补足截图夹具与命名矩阵，再让后续 token 与组件迁移有可靠回归门禁。

Purpose: 落实 D-11，并遵守研究结论“先扩视觉夹具，再做大范围基线刷新”，避免后续每次视觉修改都依赖当前登录态或临时页面状态。
Output: 一个内部 fixture route、统一 screenshot 样式层，以及覆盖关键界面的 Playwright 基线入口。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/999.2-design-md/999.2-CONTEXT.md
@.planning/phases/999.2-design-md/999.2-UI-SPEC.md
@.planning/phases/999.2-design-md/999.2-RESEARCH.md
@src/App.tsx
@src/components/auth/view/ProtectedRoute.tsx
@playwright.config.ts
@tests/visual/baseline.spec.ts

<interfaces>
From src/App.tsx:
```tsx
<Router>
  <Routes>
    <Route path='/__visual/design-md' element={<VisualRegressionFixtures />} />
    <Route path='/*' element={<ProtectedRoute><AppContent /></ProtectedRoute>} />
  </Routes>
</Router>
```

From src/components/auth/view/ProtectedRoute.tsx:

```tsx
if (!user) {
  return <LoginForm />;
}
```

当前认证守卫包在 Router 外层时，fixture 入口无法仅通过“新增一条路由”访问；执行时必须把 fixture route 放到认证边界之外，或为它增加固定且受限的 bypass。

```tsx
<Routes>
  <Route path="/" element={<AppContent />} />
  <Route path="/session/:sessionId" element={<AppContent />} />
</Routes>
```

新增的 fixture route 必须复用现有 Router，而不是引入第二套路由基础设施。
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 建立内部视觉夹具路由与稳定场景</name>
  <files>src/App.tsx, src/components/auth/view/ProtectedRoute.tsx, src/components/design-system/view/VisualRegressionFixtures.tsx</files>
  <behavior>
    - Test 1: 访问内部 fixture route 时，能稳定渲染 landing、auth、tabs、dialog、chat、settings、upgrade 场景。
    - Test 2: 默认业务入口不受影响，普通 `/` 与 `/session/:sessionId` 路由行为保持不变。
  </behavior>
  <action>在 `src/App.tsx` 重排路由所有权：把 `Router` 提到 `ProtectedRoute` 外层，仅让真实业务路由继续包在 `ProtectedRoute` 中，同时新增仅供本地与 Playwright 使用的内部 fixture route，例如 `/__visual/design-md`。如果需要最小化改动，也可以在 `ProtectedRoute` 中对该固定路径增加显式、受限的 bypass，但必须限定为这一条 fixture 入口，不能放宽普通业务页面的认证要求。新增 `src/components/design-system/view/VisualRegressionFixtures.tsx` 作为稳定场景编排页，直接组合现有 shared primitives 与高频页面壳层，显式提供 light/dark、dialog-open、landing-home、chat-composer、settings-shell、version-upgrade 等状态。不要依赖当前认证态、远端数据或用户本地存储才能渲染。对需要状态的场景，使用本地静态 view model 或最小假数据，目的是让截图输入可重复，而不是复制业务逻辑。</action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <done>Playwright 可以通过单一 fixture route 访问关键视觉面；主业务路由不回归。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 重构 Playwright 基线为命名矩阵并加入稳定截图样式</name>
  <files>tests/visual/baseline.spec.ts, tests/visual/screenshot.css, playwright.config.ts</files>
  <behavior>
    - Test 1: `npm run test:visual -- --list` 能列出面向 999.2 的命名截图用例。
    - Test 2: 截图测试默认应用统一 screenshot CSS，减少 caret、动画和滚动差异噪声。
  </behavior>
  <action>把现有 `tests/visual/baseline.spec.ts` 从“当前页面可见什么就截什么”的条件式截图，改成基于 fixture route 的命名截图矩阵；新增 `tests/visual/screenshot.css`，在截图时屏蔽非本阶段关心的动态闪烁、caret、过渡噪声，并在 `playwright.config.ts` 中接入 `expect.toHaveScreenshot` 默认配置。此任务只扩覆盖与稳定性，不做大规模快照刷新；保留后续计划使用同一矩阵做 targeted verify 和最终 refresh 的能力。</action>
  <verify>
    <automated>npm run test:visual -- --list</automated>
  </verify>
  <done>视觉用例以夹具命名矩阵为中心组织，后续计划可以按场景 grep 定向回归。</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                      | Description                             |
| ----------------------------- | --------------------------------------- |
| fixture route → browser       | 仅测试/开发可见的稳定视觉数据进入渲染层 |
| playwright runner → app shell | 截图流程依赖可预测页面状态              |

## STRIDE Threat Register

| Threat ID  | Category | Component                                                      | Disposition | Mitigation Plan                                                    |
| ---------- | -------- | -------------------------------------------------------------- | ----------- | ------------------------------------------------------------------ |
| T-999.2-01 | T        | tests/visual/baseline.spec.ts                                  | mitigate    | 固定 fixture route 和 screenshot CSS，避免测试输入被运行态波动篡改 |
| T-999.2-02 | I        | src/components/design-system/view/VisualRegressionFixtures.tsx | mitigate    | 夹具只使用本地静态假数据，不拉取真实用户或凭据数据                 |
| T-999.2-03 | D        | playwright.config.ts                                           | mitigate    | 使用统一截图默认项，避免随机动画或加载时序导致回归门禁失真         |

</threat_model>

<verification>
- `npm run typecheck`
- `npm run test:visual -- --list`
</verification>

<success_criteria>

- Phase 999.2 的后续视觉迁移都可以复用同一截图夹具入口。
- 截图用例按场景命名，可按 shell/chat/settings/auth 定向运行。
- 没有再依赖“当前页面刚好可见某个按钮/弹窗”来判断是否截图。
  </success_criteria>

<output>
After completion, create `.planning/phases/999.2-design-md/999.2-01-SUMMARY.md`
</output>
