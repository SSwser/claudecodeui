---
phase: 999.2-design-md
plan: '02'
type: execute
wave: 1
depends_on: []
files_modified:
  - src/index.css
  - tailwind.config.js
  - index.html
  - public/fonts/Inter-Variable.woff2
  - public/fonts/GeistMono-Variable.woff2
autonomous: true
requirements:
  - BACKLOG-999.2-01
must_haves:
  truths:
    - light 和 dark 模式都使用 DESIGN.md 对齐后的 token 值
    - 品牌红与 destructive 红分离，字体、阴影、圆角、间距都有可复用 token
    - 后续组件只需消费 Tailwind 语义 utility，而不是再硬编码视觉值
  artifacts:
    - src/index.css
    - tailwind.config.js
    - public/fonts/Inter-Variable.woff2
    - public/fonts/GeistMono-Variable.woff2
  key_links:
    - tailwind.config.js maps CSS variables from src/index.css into reusable utilities
    - index.html preloads self-hosted fonts used by src/index.css
---

<objective>
完成 999.2 的 token、字体和 Tailwind 桥接基础，让所有后续页面与共享组件都建立在同一套视觉事实源上。

Purpose: 覆盖 D-01、D-02、D-03、D-04、D-05、D-06、D-07、D-12、D-14、D-16、D-17，并落实 D-10 的“token 先行”。
Output: 新的 CSS 变量、字体加载管线、Tailwind 自定义颜色/阴影/圆角/字距/字体族桥接。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/999.2-design-md/999.2-CONTEXT.md
@.planning/phases/999.2-design-md/999.2-UI-SPEC.md
@.planning/phases/999.2-design-md/999.2-RESEARCH.md
@src/index.css
@tailwind.config.js
@index.html
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 替换 DESIGN.md token 并接入本地字体资源</name>
  <files>src/index.css, index.html, public/fonts/Inter-Variable.woff2, public/fonts/GeistMono-Variable.woff2</files>
  <behavior>
    - Test 1: `.dark` 与 `:root` 都输出新的背景、卡片、边框、muted、brand、ring、radius、spacing、shadow token。
    - Test 2: body 使用自托管 Inter，代码/终端相关 surface 可消费 GeistMono；字体资源不依赖网络请求。
  </behavior>
  <action>在 `src/index.css` 直接替换 light/dark 语义 token 到 DESIGN.md 对齐值，新增 `--brand` / `--brand-foreground`、多级 `--radius-*`、3 级阴影、px spacing token、字重 token 和 OpenType/letter-spacing 相关辅助 class；通过 `@font-face` 接入 `public/fonts/Inter-Variable.woff2` 与 `public/fonts/GeistMono-Variable.woff2`，并在 `index.html` 增加 preload。保持现有语义变量架构，不引入平行 token store，也不要保留旧值 shim；必要时在 CSS 注释中说明这是 D-17 要求的一步到位替换。</action>
  <verify>
    <automated>npm run typecheck && npm run build</automated>
  </verify>
  <done>全局 token 与字体管线已切到 DESIGN.md 约束，light/dark 都有明确值且没有旧 token 兼容层。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 扩展 Tailwind 语义桥接到 brand、shadow、radius、tracking 和字体族</name>
  <files>tailwind.config.js</files>
  <behavior>
    - Test 1: 组件可通过 `bg-brand`、`shadow-button`、`rounded-pill`、`tracking-ui`、`font-sans` 等 utility 直接消费新视觉 contract。
    - Test 2: spacing、radius、shadow 与字体 utility 的来源都指向 CSS variable，而不是硬编码值。
  </behavior>
  <action>在 `tailwind.config.js` 的现有 `theme.extend` 基础上新增 brand token、fontFamily、letterSpacing、boxShadow、borderRadius 与必要的 spacing 别名，全部桥接到 `src/index.css` 的 CSS 变量。不要在本计划中引入 Tailwind v4 `@theme` 或第二套主题系统；保持 v3 配置方式，以减少 churn 并与 Phase 4 保持连续性。</action>
  <verify>
    <automated>npm run typecheck && npm run build</automated>
  </verify>
  <done>后续组件和页面可只依赖语义 utility 完成视觉迁移，不需要再手写 DESIGN.md 原始值。</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                              | Description                          |
| ------------------------------------- | ------------------------------------ |
| CSS token source → all UI surfaces    | 全局变量修改会直接影响所有页面和组件 |
| local font assets → browser rendering | 字体资源加载会改变布局与截图输出     |

## STRIDE Threat Register

| Threat ID  | Category | Component             | Disposition | Mitigation Plan                                              |
| ---------- | -------- | --------------------- | ----------- | ------------------------------------------------------------ |
| T-999.2-04 | T        | src/index.css         | mitigate    | 一次性替换到锁定 token，避免旧新值并存导致视觉状态被意外混用 |
| T-999.2-05 | I        | public/fonts/\*.woff2 | mitigate    | 使用本地字体资源，不走第三方 CDN，避免额外网络暴露           |
| T-999.2-06 | D        | tailwind.config.js    | mitigate    | 保持现有 v3 语义桥接模式，降低构建配置失稳风险               |

</threat_model>

<verification>
- `npm run typecheck && npm run build`
</verification>

<success_criteria>

- `src/index.css` 成为 999.2 唯一视觉事实源。
- Tailwind 已能表达 brand、pill radius、subtle/ring/button shadow、body/ui tracking 与字体族。
- 字体和 token 改动不会引入额外外部依赖。
  </success_criteria>

<output>
After completion, create `.planning/phases/999.2-design-md/999.2-02-SUMMARY.md`
</output>
