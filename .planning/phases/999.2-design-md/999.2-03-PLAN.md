---
phase: 999.2-design-md
plan: '03'
type: execute
wave: 2
depends_on:
  - 999.2-01
  - 999.2-02
files_modified:
  - src/shared/view/ui/Button.tsx
  - src/shared/view/ui/Input.tsx
  - src/shared/view/ui/Badge.tsx
  - src/components/ui/button.tsx
  - src/components/ui/input.tsx
  - src/components/ui/dialog.tsx
  - src/components/ui/select.tsx
autonomous: true
requirements:
  - BACKLOG-999.2-01
must_haves:
  truths:
    - Button 变体已按 UI-SPEC 重映射，但调用 API 保持兼容
    - Input、Badge、Dialog、Select 共享 DESIGN.md 边框、圆角、阴影和 focus 语言
    - opacity-first hover 成为共享 primitives 的默认交互模式
  artifacts:
    - src/shared/view/ui/Button.tsx
    - src/shared/view/ui/Input.tsx
    - src/shared/view/ui/Badge.tsx
    - src/components/ui/dialog.tsx
    - src/components/ui/select.tsx
  key_links:
    - shared Button variant API stays stable while visuals change underneath
    - dialog/select consume new token utilities instead of hardcoded blue/gray fills
---

<objective>
先收敛 shared primitives，再让后续 feature 页面只做“应用入口”而不是继续发明样式。

Purpose: 覆盖 D-07、D-08、D-09、D-13、D-15，并落实“shared primitive changes ahead of feature-by-feature cleanup”。
Output: 保持 API 兼容但视觉契约重写后的 Button、Input、Badge、Dialog、Select。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/999.2-design-md/999.2-CONTEXT.md
@.planning/phases/999.2-design-md/999.2-UI-SPEC.md
@.planning/phases/04-ui-migration/04-01-SUMMARY.md
@src/shared/view/ui/Button.tsx
@src/shared/view/ui/Input.tsx
@src/shared/view/ui/Badge.tsx
@src/components/ui/dialog.tsx
@src/components/ui/select.tsx

<interfaces>
From src/shared/view/ui/Button.tsx:
```tsx
type ButtonProps = React.ButtonHTMLAttributes<HTMLButtonElement> &
  VariantProps<typeof buttonVariants>;
```

这个对外 API 不能破坏；视觉映射必须在现有 `variant` / `size` 合同内完成。
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 重映射 Button、Input、Badge 到 DESIGN.md primitive contract</name>
  <files>src/shared/view/ui/Button.tsx, src/shared/view/ui/Input.tsx, src/shared/view/ui/Badge.tsx, src/components/ui/button.tsx, src/components/ui/input.tsx</files>
  <behavior>
    - Test 1: Button 的 `default`、`secondary`、`ghost`、`outline`、`destructive` 视觉契约与 UI-SPEC 对齐，但 props API 不变。
    - Test 2: Input 与 Badge 使用新的 radius、shadow、focus-visible、muted/brand/destructive token，不再依赖旧的蓝灰 hover 语言。
  </behavior>
  <action>在 shared primitives 中重写基础类，使用 `rounded-pill`、`shadow-button`、`shadow-ring`、`tracking-ui`、`font-medium`、`hover:opacity-60` 等新 utility。`Button` 的 variant 语义保持兼容，但样式映射到 Primary Pill / Secondary / Ghost / destructive contract；`Input` 使用 dark field fill、blue ring focus 与 medium radius；`Badge` 统一中性色壳层，只有显式 destructive 或成功语义保留对应状态色。桥接 re-export 文件只在必要时同步类型或导出，不要复制样式逻辑。</action>
  <verify>
    <automated>npm run typecheck && npm run test:visual -- --grep "fixture|auth|landing|dialog"</automated>
  </verify>
  <done>共享 Button/Input/Badge 已成为后续页面迁移的唯一视觉入口。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 统一 Dialog 与 Select 的边框、阴影和焦点语言</name>
  <files>src/components/ui/dialog.tsx, src/components/ui/select.tsx</files>
  <behavior>
    - Test 1: Dialog overlay、panel、close affordance 使用新 card shell、double-ring 和 opacity hover，而不是旧的蓝灰背景交互。
    - Test 2: Select trigger 与 listbox 采用相同 token contract，并保留键盘无障碍行为。
  </behavior>
  <action>更新 `src/components/ui/dialog.tsx` 与 `src/components/ui/select.tsx`，让 overlay、panel、trigger、option highlight、focus ring 与边框全部走 DESIGN.md 语义 token。保留 Phase 4 提供的 portal、Escape 和 listbox 语义；只改变视觉 contract，不改无障碍和事件行为。若需要新增轻量 cva 或共享 class 常量，可以在这两个文件内完成，但不要把视觉逻辑散落到调用方。</action>
  <verify>
    <automated>npm run typecheck && npm run test:visual -- --grep "dialog|select|fixture"</automated>
  </verify>
  <done>Dialog 和 Select 与 Button/Input 共享同一边框、半径、阴影和 hover/focus 语言。</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                 | Description                      |
| ---------------------------------------- | -------------------------------- |
| shared primitives → all feature screens  | 基础组件变化会向全局扩散         |
| focus-visible semantics → keyboard users | hover 语言变化不能破坏焦点可见性 |

## STRIDE Threat Register

| Threat ID  | Category | Component                     | Disposition | Mitigation Plan                                                  |
| ---------- | -------- | ----------------------------- | ----------- | ---------------------------------------------------------------- |
| T-999.2-07 | S        | src/shared/view/ui/Button.tsx | mitigate    | 保持 destructive 与 brand 分离，避免危险操作与 CTA 视觉混淆      |
| T-999.2-08 | D        | src/components/ui/dialog.tsx  | mitigate    | 只改视觉 contract，不破坏 portal、Escape 和 focus 行为           |
| T-999.2-09 | D        | src/components/ui/select.tsx  | mitigate    | 保留现有 combobox/listbox 语义和键盘导航，避免因样式迁移损坏交互 |

</threat_model>

<verification>
- `npm run typecheck`
- `npm run test:visual -- --grep "dialog|select|fixture|auth|landing"`
</verification>

<success_criteria>

- 页面迁移不再需要手写自定义按钮/输入框主样式。
- opacity-first hover 与蓝色 focus ring 已在共享 primitives 层落地。
- 调用方 API 与 Phase 4 迁移结果保持兼容。
  </success_criteria>

<output>
After completion, create `.planning/phases/999.2-design-md/999.2-03-SUMMARY.md`
</output>
