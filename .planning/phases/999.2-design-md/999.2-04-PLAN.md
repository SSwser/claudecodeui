---
phase: 999.2-design-md
plan: '04'
type: execute
wave: 3
depends_on:
  - 999.2-01
  - 999.2-02
  - 999.2-03
files_modified:
  - src/components/home/view/LandingPage.tsx
  - src/components/home/view/HomeFilters.tsx
  - src/components/home/view/FavoritesSection.tsx
  - src/components/home/view/RecentSessionsList.tsx
  - src/components/app/view/AppTabStrip.tsx
autonomous: true
requirements:
  - BACKLOG-999.2-01
must_haves:
  truths:
    - 用户一打开 landing 与 tabs 就能看到新的 Raycast 风格视觉语言
    - home 与 tabs 不再依赖蓝色渐变、蓝按钮和旧 hover 规则
    - 壳层级页面优先使用共享 primitives，而不是继续写局部按钮/输入样式
  artifacts:
    - src/components/home/view/LandingPage.tsx
    - src/components/home/view/HomeFilters.tsx
    - src/components/app/view/AppTabStrip.tsx
  key_links:
    - LandingPage and HomeFilters consume shared Button/Input instead of local blue styles
    - AppTabStrip adopts the new tab chrome expected by the fixture screenshots
---

<objective>
把最先被用户看到的 home 与 tab shell 拉到同一视觉体系，作为 999.2 的第一批真实产品界面校准面。

Purpose: 用最小返工路径验证 token 与 primitives 是否足够支撑真实页面，并清掉研究中点名的 landing/tabs 旧蓝灰样式。
Output: 已迁移的 home/app shell 页面集合。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/999.2-design-md/999.2-UI-SPEC.md
@src/components/home/view/LandingPage.tsx
@src/components/home/view/HomeFilters.tsx
@src/components/home/view/FavoritesSection.tsx
@src/components/home/view/RecentSessionsList.tsx
@src/components/app/view/AppTabStrip.tsx

<interfaces>
From src/components/home/view/LandingPage.tsx:
```tsx
export type LandingPageViewModel = {
  filters: { search: string; project: string | null; workspace: string | null; sessionType: string };
  favoriteWorkspaces: FavoriteWorkspaceCard[];
  favoriteSessions: FavoriteSessionCard[];
  recentSessions: RecentSessionItem[];
  projectOptions: SelectOption[];
  workspaceOptions: SelectOption[];
};
```

保持 view model contract 稳定，迁移只改变视觉表达与 primitive 入口使用方式。
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 统一 landing 与 tab shell 的卡片、按钮、过滤区和 tab chrome</name>
  <files>src/components/home/view/LandingPage.tsx, src/components/home/view/HomeFilters.tsx, src/components/home/view/FavoritesSection.tsx, src/components/home/view/RecentSessionsList.tsx, src/components/app/view/AppTabStrip.tsx</files>
  <behavior>
    - Test 1: landing hero、收藏区、最近会话和过滤区不再使用蓝色渐变主导，而改为 DESIGN.md 背景/卡片/brand 标点语言。
    - Test 2: tabs 使用新的 pill/ring/opacity hover contract，选中态与未选中态层次明确但不回到旧蓝色 hover 模式。
  </behavior>
  <action>迁移 landing 与 tab shell 的壳层 chrome，移除研究中指出的蓝色 radial gradient、`hover:border-primary/35`、旧 rounded-2xl 拼贴感，改用新的 page background、card shell、brand punctuation、tracking-ui 与 opacity-first hover。尽可能复用 Plan 03 已完成的 Button/Select/Input；如果某个页面仍在自带按钮或输入壳层，优先替换为共享 primitive 而不是继续局部修补。</action>
  <verify>
    <automated>npm run typecheck && npm run test:visual -- --grep "landing|tabs|home"</automated>
  </verify>
  <done>home 与 tabs 成为 999.2 第一批真实新视觉页面，且不再依赖旧蓝色风格。</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                        | Description                              |
| ------------------------------- | ---------------------------------------- |
| auth UI → credential entry      | 视觉迁移不能模糊提交、错误和禁用状态     |
| home shell → navigation actions | tab/landing 迁移不能破坏可点击和可区分性 |

## STRIDE Threat Register

| Threat ID  | Category | Component                                   | Disposition | Mitigation Plan                                                            |
| ---------- | -------- | ------------------------------------------- | ----------- | -------------------------------------------------------------------------- |
| T-999.2-10 | S        | src/components/auth/view/LoginForm.tsx      | mitigate    | 使用新的 CTA contract 但保留明显提交 affordance，不让主操作与次级操作混淆  |
| T-999.2-11 | D        | src/components/app/view/AppTabStrip.tsx     | mitigate    | 只调整视觉层次与 hover contract，不破坏 tab 选择、关闭和 context menu 事件 |
| T-999.2-12 | D        | src/components/auth/view/AuthErrorAlert.tsx | mitigate    | destructive 语义继续显式保留，避免错误反馈被 brand 色吞没                  |

</threat_model>

<verification>
- `npm run typecheck`
- `npm run test:visual -- --grep "landing|tabs|home"`
</verification>

<success_criteria>

- landing、tabs 都能被 fixture 截图稳定验证。
- 这些壳层页面不再包含阶段前的蓝色按钮/渐变主视觉。
- 共享 primitives 成为页面交互样式的主要入口。
  </success_criteria>

<output>
After completion, create `.planning/phases/999.2-design-md/999.2-04-SUMMARY.md`
</output>
