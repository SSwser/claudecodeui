---
phase: 999.2-design-md
plan: '05'
type: execute
wave: 3
depends_on:
  - 999.2-01
  - 999.2-02
  - 999.2-03
files_modified:
  - src/components/chat/view/subcomponents/ChatComposer.tsx
  - src/components/chat/view/subcomponents/ChatInputControls.tsx
  - src/components/chat/view/subcomponents/ThinkingModeSelector.tsx
  - src/components/chat/view/subcomponents/ClaudeStatus.tsx
  - src/components/chat/view/subcomponents/CommandMenu.tsx
autonomous: true
requirements:
  - BACKLOG-999.2-01
must_haves:
  truths:
    - 聊天输入区、命令菜单与状态条遵守同一套新视觉语言
    - 聊天核心页面不再被旧蓝灰 hardcode 主导
    - composer 与 controls 的 hover、focus、badge 与状态强调都回到同一套 token contract
  artifacts:
    - src/components/chat/view/subcomponents/ChatComposer.tsx
    - src/components/chat/view/subcomponents/ChatInputControls.tsx
    - src/components/chat/view/subcomponents/ThinkingModeSelector.tsx
    - src/components/chat/view/subcomponents/CommandMenu.tsx
  key_links:
    - ChatComposer and ChatInputControls consume the shared button/input contracts
    - ThinkingModeSelector and command surfaces stop using the old blue/gray local theme
---

<objective>
统一聊天输入主工作面板，让最核心的 composer、controls、status 与命令菜单进入 DESIGN.md 的视觉系统。

Purpose: 先把高交互密度的 chat controls 收敛到 token/primitives，避免在消息渲染层稳定前反复横跳。
Output: 已迁移的 chat composer、menus、status 与 control surfaces。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/999.2-design-md/999.2-UI-SPEC.md
@src/components/chat/view/subcomponents/ChatComposer.tsx
@src/components/chat/view/subcomponents/ChatInputControls.tsx
@src/components/chat/view/subcomponents/ThinkingModeSelector.tsx
@src/components/chat/view/subcomponents/ClaudeStatus.tsx
@src/components/chat/view/subcomponents/CommandMenu.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 统一 composer、controls、status 与 command menu 的容器和交互语言</name>
  <files>src/components/chat/view/subcomponents/ChatComposer.tsx, src/components/chat/view/subcomponents/ChatInputControls.tsx, src/components/chat/view/subcomponents/ThinkingModeSelector.tsx, src/components/chat/view/subcomponents/ClaudeStatus.tsx, src/components/chat/view/subcomponents/CommandMenu.tsx</files>
  <behavior>
    - Test 1: composer 输入壳层、附件区、下拉菜单和状态条不再使用旧蓝色 hover/背景逻辑，而是走 card shell、ring、opacity hover 和 blue focus ring。
    - Test 2: command menu 与 mode/status pills 的强调色有语义分层，但不回退成大面积蓝色实底。
  </behavior>
  <action>重写 composer 与相关 controls/status/menu 的容器、按钮、下拉和 badge 样式，优先复用 Plan 03 的 shared Button/Input/Dialog/Select contract。显式把 `ThinkingModeSelector.tsx` 以及 `ChatInputControls.tsx` 中的 design mode 按钮一起纳入，因为它们就在主输入调用链上且仍保留 blue/gray 旧主题。移除 `bg-blue-*`、`hover:bg-blue-*`、`bg-white dark:bg-gray-*` 等旧模式，改用新 token、ring shadow、medium/large radius 与 opacity hover。保留语义色用于正在运行、警告、权限等状态，但要通过语义 token 与中性外壳承载，而不是每个块自带一套蓝灰主题。</action>
  <verify>
    <automated>npm run typecheck && npm run test:visual -- --grep "chat|composer|command|status"</automated>
  </verify>
  <done>聊天输入与状态控制面板在视觉上已与 landing/auth/settings 处于同一设计语言。</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                              | Description                                          |
| ------------------------------------- | ---------------------------------------------------- |
| chat UI → user actions                | 视觉迁移不能削弱发送、权限确认、错误反馈和链接可读性 |
| markdown renderer → untrusted content | 不可信内容渲染的视觉外壳需要保持清晰而不过度装饰     |

## STRIDE Threat Register

| Threat ID  | Category | Component                                                   | Disposition | Mitigation Plan                                                              |
| ---------- | -------- | ----------------------------------------------------------- | ----------- | ---------------------------------------------------------------------------- |
| T-999.2-13 | S        | src/components/chat/view/subcomponents/MessageComponent.tsx | mitigate    | brand、destructive、warning 继续分离，避免用户把错误或权限消息误判为普通 CTA |
| T-999.2-14 | D        | src/components/chat/view/subcomponents/Markdown.tsx         | mitigate    | 只改渲染壳层与 token，不改变 markdown 解析和 link 安全属性                   |
| T-999.2-15 | D        | src/components/chat/view/subcomponents/ChatComposer.tsx     | mitigate    | 保持输入、附件、提交与快捷控制交互逻辑不变，只迁移视觉 contract              |

</threat_model>

<verification>
- `npm run typecheck`
- `npm run test:visual -- --grep "chat|composer|messages|markdown|permission|status"`
</verification>

<success_criteria>

- chat composer、controls、status 与 command menu 完成视觉统一。
- 关键状态强调仍清晰可辨，但样式来源统一回到 token/primitives。
- fixture 可以稳定覆盖 composer、menu、mode selector 与 status 等核心控件。
  </success_criteria>

<output>
After completion, create `.planning/phases/999.2-design-md/999.2-05-SUMMARY.md`
</output>
