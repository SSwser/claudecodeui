---
phase: 999.2-design-md
plan: '06'
type: execute
wave: 3
depends_on:
  - 999.2-01
  - 999.2-02
  - 999.2-03
files_modified:
  - src/components/settings/view/SettingsCard.tsx
  - src/components/settings/view/SettingsMainTabs.tsx
  - src/components/settings/view/SettingsSidebar.tsx
  - src/components/settings/view/tabs/NotificationsSettingsTab.tsx
  - src/components/settings/view/tabs/tasks-settings/TasksSettingsTab.tsx
autonomous: true
requirements:
  - BACKLOG-999.2-01
must_haves:
  truths:
    - settings 壳层、tabs 与常见说明卡与新视觉系统一致
    - 蓝/灰硬编码被替换为 brand、ring、muted 等语义 token
    - 带语义提示的设置说明块仍保留提示能力，但视觉来源统一
  artifacts:
    - src/components/settings/view/SettingsCard.tsx
    - src/components/settings/view/SettingsMainTabs.tsx
    - src/components/settings/view/SettingsSidebar.tsx
    - src/components/settings/view/tabs/tasks-settings/TasksSettingsTab.tsx
  key_links:
    - settings shell and tabs reuse shared primitives instead of local blue/gray buttons
    - notifications and tasks surfaces become the canonical shell fixtures for later modal work
---

<objective>
统一 settings 壳层、tabs、通知页和任务设置页，把基础配置界面先拉回同一视觉语言。

Purpose: 先稳定 settings 基础壳层，让后续 modal、permission 与 upgrade 面板可以在相同 shell contract 上迭代。
Output: 已迁移的 settings shell、tabs 与高频设置页。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/999.2-design-md/999.2-UI-SPEC.md
@src/components/settings/view/SettingsCard.tsx
@src/components/settings/view/SettingsMainTabs.tsx
@src/components/settings/view/SettingsSidebar.tsx
@src/components/settings/view/tabs/NotificationsSettingsTab.tsx
@src/components/settings/view/tabs/tasks-settings/TasksSettingsTab.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 统一 settings shell、tabs 与说明卡片的基础视觉 contract</name>
  <files>src/components/settings/view/SettingsCard.tsx, src/components/settings/view/SettingsMainTabs.tsx, src/components/settings/view/SettingsSidebar.tsx, src/components/settings/view/tabs/NotificationsSettingsTab.tsx, src/components/settings/view/tabs/tasks-settings/TasksSettingsTab.tsx</files>
  <behavior>
    - Test 1: settings 壳层卡片、主 tabs 和说明块使用新的 radius、double-ring、muted text 与 opacity hover 语言。
    - Test 2: 通知、任务设置等页面中的主次按钮不再依赖本地蓝色/灰色按钮类。
  </behavior>
  <action>把 settings 基础容器、tabs 与常见说明块迁移到新视觉系统，优先使用共享 Button/Input/Badge 与 token utility。对于提示性 callout，允许保留 warning/destructive/info 语义色，但容器底色、边框、字体与 hover/focus 必须统一到 DESIGN.md contract；不要再使用旧的蓝色 tab 边框和灰底按钮体系。</action>
  <verify>
    <automated>npm run typecheck && npm run test:visual -- --grep "settings|tabs|notifications|tasks"</automated>
  </verify>
  <done>settings 作为配置壳层已脱离旧蓝灰视觉系统。</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                            | Description                                        |
| ----------------------------------- | -------------------------------------------------- |
| settings UI → configuration changes | 风格迁移不能降低表单、权限和危险操作的辨识度       |
| upgrade modal → update action       | 视觉统一不能误导用户对升级/关闭/复制命令按钮的理解 |

## STRIDE Threat Register

| Threat ID  | Category | Component                                                                                 | Disposition | Mitigation Plan                                                                            |
| ---------- | -------- | ----------------------------------------------------------------------------------------- | ----------- | ------------------------------------------------------------------------------------------ |
| T-999.2-16 | S        | src/components/version-upgrade/view/VersionUpgradeModal.tsx                               | mitigate    | 保留主升级动作、次级关闭动作与错误态之间的强区分，不让 brand 取代 destructive/primary 语义 |
| T-999.2-17 | D        | src/components/settings/view/modals/ClaudeMcpFormModal.tsx                                | mitigate    | 保持现有表单与验证逻辑，只替换视觉契约和共享控件入口                                       |
| T-999.2-18 | S        | src/components/settings/view/tabs/agents-settings/sections/content/PermissionsContent.tsx | mitigate    | 语义提示块继续保留 allowed/blocked/warning 差异，防止权限状态被视觉统一冲淡                |

</threat_model>

<verification>
- `npm run typecheck`
- `npm run test:visual -- --grep "settings|tabs|notifications|tasks"`
</verification>

<success_criteria>

- settings shell、tabs、notifications 和 tasks 页面完成视觉统一。
- 基础配置界面不再各自维护私有蓝灰主题体系。
- 后续 modal 与升级面板可以直接复用已经稳定的 settings shell contract。
  </success_criteria>

<output>
After completion, create `.planning/phases/999.2-design-md/999.2-06-SUMMARY.md`
</output>
