---
phase: 999.2-design-md
plan: '07'
type: execute
wave: 4
depends_on:
  - 999.2-05
files_modified:
  - src/components/chat/view/subcomponents/ChatMessagesPane.tsx
  - src/components/chat/view/subcomponents/MessageComponent.tsx
  - src/components/chat/view/subcomponents/Markdown.tsx
  - src/components/chat/view/subcomponents/PermissionRequestsBanner.tsx
  - src/components/chat/view/subcomponents/TokenUsagePie.tsx
autonomous: true
requirements:
  - BACKLOG-999.2-01
must_haves:
  truths:
    - 普通消息、系统消息、权限横幅与 markdown 元素遵守同一套新视觉语言
    - 聊天消息层不再被旧蓝灰 hardcode 主导
    - 信息、警告、错误和权限语义仍可区分，但容器 chrome 与 typography 规则统一
  artifacts:
    - src/components/chat/view/subcomponents/ChatMessagesPane.tsx
    - src/components/chat/view/subcomponents/MessageComponent.tsx
    - src/components/chat/view/subcomponents/Markdown.tsx
    - src/components/chat/view/subcomponents/PermissionRequestsBanner.tsx
  key_links:
    - message list and markdown rendering adopt semantic shells and text tokens instead of local blue/gray rules
    - permission and token-usage surfaces align with the chat controls contract established in Plan 05
---

<objective>
统一聊天消息渲染层，让 message list、markdown、permission banner 和 token usage 读数进入 DESIGN.md 的视觉系统。

Purpose: 在 chat controls 稳定后，再处理消息壳层与 markdown 渲染，减少同一计划内同时调整输入区与渲染区带来的返工。
Output: 已迁移的 chat messages、markdown 与 permission surfaces。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/999.2-design-md/999.2-UI-SPEC.md
@src/components/chat/view/subcomponents/ChatMessagesPane.tsx
@src/components/chat/view/subcomponents/MessageComponent.tsx
@src/components/chat/view/subcomponents/Markdown.tsx
@src/components/chat/view/subcomponents/PermissionRequestsBanner.tsx
@src/components/chat/view/subcomponents/TokenUsagePie.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 统一消息渲染、Markdown、权限横幅和 token 读数的语义外壳</name>
  <files>src/components/chat/view/subcomponents/ChatMessagesPane.tsx, src/components/chat/view/subcomponents/MessageComponent.tsx, src/components/chat/view/subcomponents/Markdown.tsx, src/components/chat/view/subcomponents/PermissionRequestsBanner.tsx, src/components/chat/view/subcomponents/TokenUsagePie.tsx</files>
  <behavior>
    - Test 1: 普通消息、系统消息、错误消息、权限消息与 markdown 元素都落在新的中性 surface + 语义强调 contract 上。
    - Test 2: 链接、代码块、表格、引用、inline code 与消息操作菜单不再散落旧的 gray/blue 样式体系。
  </behavior>
  <action>把消息列表与 Markdown 子元素改成基于 semantic token 的背景、边框、文本与链接风格；保留错误、警告、成功、权限等语义区分，但将其承载在统一的 card shell、border、radius 和 typography 系统中。对 `MessageComponent` 与 `PermissionRequestsBanner` 中仍然存在的大块红/黄/蓝背景、`hover:bg-white`、`text-blue-*` 等样式做集中清理；对 `Markdown` 的表格、代码块、引用和链接建立一致的 DESIGN.md contract。`TokenUsagePie` 的读数与环形辅助文本也应切到 muted/foreground/ring 体系。</action>
  <verify>
    <automated>npm run typecheck && npm run test:visual -- --grep "chat|messages|markdown|permission"</automated>
  </verify>
  <done>聊天消息与 markdown 呈现不再是 Phase 999.2 前的旧风格孤岛。</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                         | Description                                      |
| ------------------------------------------------ | ------------------------------------------------ |
| chat UI → message reading and permission actions | 视觉迁移不能削弱消息层级、权限确认和链接可读性   |
| markdown renderer → untrusted content            | 不可信内容渲染的视觉外壳需要保持清晰而不过度装饰 |

## STRIDE Threat Register

| Threat ID  | Category | Component                                                           | Disposition | Mitigation Plan                                                              |
| ---------- | -------- | ------------------------------------------------------------------- | ----------- | ---------------------------------------------------------------------------- |
| T-999.2-19 | S        | src/components/chat/view/subcomponents/MessageComponent.tsx         | mitigate    | brand、destructive、warning 继续分离，避免用户把错误或权限消息误判为普通 CTA |
| T-999.2-20 | D        | src/components/chat/view/subcomponents/Markdown.tsx                 | mitigate    | 只改渲染壳层与 token，不改变 markdown 解析和 link 安全属性                   |
| T-999.2-21 | D        | src/components/chat/view/subcomponents/PermissionRequestsBanner.tsx | mitigate    | 权限横幅继续保留显式 allowed/blocked/warning 差异，避免视觉统一冲淡风险提示  |

</threat_model>

<verification>
- `npm run typecheck`
- `npm run test:visual -- --grep "chat|messages|markdown|permission|status"`
</verification>

<success_criteria>

- chat 消息层与 markdown 呈现完成视觉统一。
- 权限、错误、普通消息和代码块仍清晰可辨，但样式来源统一回到 token/primitives。
- fixture 可以稳定覆盖 message list、permission banner 与 markdown rendering 等核心场景。
  </success_criteria>

<output>
After completion, create `.planning/phases/999.2-design-md/999.2-07-SUMMARY.md`
</output>
