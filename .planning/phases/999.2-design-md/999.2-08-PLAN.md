---
phase: 999.2-design-md
plan: '08'
type: execute
wave: 3
depends_on:
  - 999.2-01
  - 999.2-02
  - 999.2-03
files_modified:
  - src/components/auth/view/AuthScreenLayout.tsx
  - src/components/auth/view/AuthInputField.tsx
  - src/components/auth/view/LoginForm.tsx
  - src/components/auth/view/SetupForm.tsx
  - src/components/auth/view/AuthErrorAlert.tsx
  - src/components/auth/view/AuthLoadingScreen.tsx
autonomous: true
requirements:
  - BACKLOG-999.2-01
must_haves:
  truths:
    - auth 页面、错误态与加载态都遵守新的 token、radius、shadow 与 typography contract
    - `AuthInputField` 不再遗留 blue focus ring，真实认证调用链没有旧输入壳层漏口
    - 主 CTA、错误态和禁用态仍然清晰可辨
  artifacts:
    - src/components/auth/view/AuthScreenLayout.tsx
    - src/components/auth/view/AuthInputField.tsx
    - src/components/auth/view/LoginForm.tsx
    - src/components/auth/view/SetupForm.tsx
  key_links:
    - login/setup screens consume shared Button/Input instead of local blue styles
    - auth error and loading surfaces adopt the same destructive/muted hierarchy as the rest of the app
---

<objective>
统一 auth 页面、错误态与加载态到新 token 和 primitive，避免认证流继续作为旧视觉孤岛存在。

Purpose: 把 auth 从 home/tabs 计划中拆出来，减少返工面，同时显式修掉 `AuthInputField` 这条真实调用链漏口。
Output: 已迁移的 auth 页面集合。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/999.2-design-md/999.2-UI-SPEC.md
@src/components/auth/view/AuthScreenLayout.tsx
@src/components/auth/view/AuthInputField.tsx
@src/components/auth/view/LoginForm.tsx
@src/components/auth/view/SetupForm.tsx
@src/components/auth/view/AuthErrorAlert.tsx
@src/components/auth/view/AuthLoadingScreen.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 统一 auth 页面、错误态与加载态到新 token 和 primitive</name>
  <files>src/components/auth/view/AuthScreenLayout.tsx, src/components/auth/view/AuthInputField.tsx, src/components/auth/view/LoginForm.tsx, src/components/auth/view/SetupForm.tsx, src/components/auth/view/AuthErrorAlert.tsx, src/components/auth/view/AuthLoadingScreen.tsx</files>
  <behavior>
    - Test 1: 登录与初始化表单的主 CTA 使用新的按钮 contract，而不是本地蓝色提交按钮。
    - Test 2: 错误态与加载态保留语义色，但容器、排版、间距和 hover/focus 走新视觉系统。
  </behavior>
  <action>把 auth 相关页面全部切到共享 Button/Input 和新的 typography/radius/shadow 体系；显式把 `AuthInputField.tsx` 纳入这一波，移除它当前遗留的 blue focus ring，确保 `LoginForm` 与 `SetupForm` 的真实调用链不会留下旧输入壳层。同步移除本地 `bg-blue-600`、`hover:bg-blue-700` 和 bounce dots 的旧视觉默认值。`AuthErrorAlert` 继续保持错误语义，但容器边框、背景和文字层级应与 UI-SPEC 的 destructive contract 对齐。`AuthLoadingScreen` 可保留动感最小化反馈，但不要引入新的全局动效系统，因为该能力已被 deferred。</action>
  <verify>
    <automated>npm run typecheck && npm run test:visual -- --grep "auth|login|setup"</automated>
  </verify>
  <done>auth 整体不再是独立旧风格岛，且 light/dark 下都使用新 token。</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                   | Description                          |
| -------------------------- | ------------------------------------ |
| auth UI → credential entry | 视觉迁移不能模糊提交、错误和禁用状态 |

## STRIDE Threat Register

| Threat ID  | Category | Component                                   | Disposition | Mitigation Plan                                                              |
| ---------- | -------- | ------------------------------------------- | ----------- | ---------------------------------------------------------------------------- |
| T-999.2-10 | S        | src/components/auth/view/LoginForm.tsx      | mitigate    | 使用新的 CTA contract 但保留明显提交 affordance，不让主操作与次级操作混淆    |
| T-999.2-11 | D        | src/components/auth/view/AuthInputField.tsx | mitigate    | blue focus ring 必须迁移到共享 input/ring contract，避免真实调用链遗留旧主题 |
| T-999.2-12 | D        | src/components/auth/view/AuthErrorAlert.tsx | mitigate    | destructive 语义继续显式保留，避免错误反馈被 brand 色吞没                    |

</threat_model>

<verification>
- `npm run typecheck`
- `npm run test:visual -- --grep "auth|login|setup"`
</verification>

<success_criteria>

- auth 能被 fixture 截图稳定验证。
- auth 页面不再包含阶段前的蓝色按钮或旧输入焦点风格。
- 认证流的 CTA、错误态和加载态仍然清晰可辨。
  </success_criteria>

<output>
After completion, create `.planning/phases/999.2-design-md/999.2-08-SUMMARY.md`
</output>
