---
phase: 999.2-design-md
plan: '10'
type: execute
wave: 5
depends_on:
  - 999.2-04
  - 999.2-05
  - 999.2-06
  - 999.2-07
  - 999.2-08
  - 999.2-09
files_modified:
  - scripts/check-visual-hardcoded-colors.cjs
  - package.json
  - playwright.config.ts
  - tests/visual/baseline.spec.ts
  - tests/visual/screenshot.css
autonomous: false
requirements:
  - BACKLOG-999.2-01
must_haves:
  truths:
    - 999.2 结束时有自动化门禁阻止关键目录回流到旧的 hardcoded 蓝灰样式
    - 所有关键视觉夹具截图已按新风格刷新并可重复运行
    - 最终 light/dark 人工巡检有明确验收步骤，不把纯视觉判断交给截图 diff 独自完成
  artifacts:
    - scripts/check-visual-hardcoded-colors.cjs
    - package.json
    - tests/visual/baseline.spec.ts
    - tests/visual/screenshot.css
  key_links:
    - package.json exposes a dedicated design-md audit command
    - final visual baselines are refreshed from the fixture route established in Plan 01
---

<objective>
给 999.2 做最后收口：先清 residual hardcode，再增加审计门禁，刷新视觉基线，并通过人工巡检确认 DESIGN.md 风格已真正落地。

Purpose: 把“完成这阶段”从一次性视觉改造，升级为可持续的回归门禁；同时遵守研究要求，先消化 residual，再做 broad baseline refresh，避免最后一个 gate 才首次暴露关键漏口。
Output: residual cleanup 收口、视觉 hardcode 审计脚本、package command、更新后的基线矩阵，以及人工验收记录入口。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/999.2-design-md/999.2-RESEARCH.md
@.planning/phases/999.2-design-md/999.2-01-PLAN.md
@tests/visual/baseline.spec.ts
@playwright.config.ts
@package.json
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 建立 design-md 审计脚本并完成 residual cleanup 收口</name>
  <files>scripts/check-visual-hardcoded-colors.cjs, package.json</files>
  <behavior>
    - Test 1: 在 home/app/auth/chat/settings/version-upgrade 目录中，新的审计脚本会报告回流的 `bg-blue-*`、`dark:bg-gray-*`、`hover:bg-blue-*` 等旧视觉 hardcode。
    - Test 2: 在最终 refresh 之前，审计脚本新发现的同目录残留项都已被修正或明确 allowlist。
    - Test 3: 语义上允许存在的 destructive/warning/success 状态不会被错误拦截。
  </behavior>
  <action>新增 `scripts/check-visual-hardcoded-colors.cjs`，对 999.2 涉及的关键目录做 targeted audit，识别旧的蓝灰主视觉类名、旧 hover 模式和不再允许的 light/dark 直写配色；把该脚本接入 `package.json`，例如 `npm run lint:design-md`。先在已经被本阶段覆盖的目录内跑一次 residual cleanup，再进入最终截图刷新。脚本应聚焦本阶段定义的“禁止回流模式”，而不是粗暴禁止一切颜色类，确保 destructive/warning/success 等语义状态仍可合法存在。若确实需要 allowlist，写在脚本内并带注释说明为什么保留。</action>
  <verify>
    <automated>npm run lint:design-md</automated>
  </verify>
  <done>关键目录在 broad snapshot refresh 前已完成 residual cleanup，并且已有自动化门禁防止旧视觉模式回流。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 基于夹具矩阵刷新最终截图并完成收口验证</name>
  <files>playwright.config.ts, tests/visual/baseline.spec.ts, tests/visual/screenshot.css</files>
  <behavior>
    - Test 1: 基线更新只基于 Plan 01 的 fixture route，不再从随机运行态截屏。
    - Test 2: 完整视觉回归与构建检查通过，给人工巡检留出明确 light/dark 检查步骤。
  </behavior>
  <action>在前面各执行计划完成后，基于 fixture route 对所有 design-md 命名截图执行最终 refresh，并清理不再需要的旧条件式截图分支。若 `playwright.config.ts` 或 `tests/visual/screenshot.css` 仍需微调来稳定最终基线，可在本任务统一完成。此任务是本阶段唯一允许做 broad baseline refresh 的位置，因为到这里 fixture、tokens、primitives 和高频页面都已稳定。</action>
  <verify>
    <automated>npm run typecheck && npm run build && npm run lint:design-md && npm run test:visual:update && npm run test:visual</automated>
  </verify>
  <done>自动化审计、最终快照刷新和完整视觉回归均已通过，进入人工验收。</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify the final design-md rollout</name>
  <files>N/A - human verification checkpoint for the completed visual-system migration</files>
  <action>Pause after the final audit and snapshot refresh, then have the user verify the branded focal points, semantic states, hover language, and light/dark consistency in a running build.</action>
  <what-built>完整的 999.2 视觉系统迁移，包括稳定截图夹具、token/primitive 重映射、壳层页面、chat、settings 和最终基线刷新。</what-built>
  <how-to-verify>
    1. 启动应用与截图所用同一构建输入，打开内部 fixture route 与主应用首页。
    2. 在 light 和 dark 下分别检查 landing、auth、tabs、chat composer、message list、settings、upgrade modal。
    3. 确认主 CTA 只在少数品牌焦点位使用 `--brand`，错误/危险态继续使用 destructive 语义。
    4. 确认 hover 语言以 opacity-first 为主，没有回到大面积背景色闪烁。
    5. 若发现任何页面仍明显保留旧蓝灰/紫色主题，记录具体场景并拒绝验收。
  </how-to-verify>
  <verify>
    <automated>npm run typecheck && npm run build && npm run lint:design-md && npm run test:visual</automated>
  </verify>
  <done>用户确认最终视觉 rollout 满足品牌、层级、hover 与 light/dark 检查清单，或返回具体页面问题以便新增 gap closure 计划。</done>
  <resume-signal>回复 `approved` 继续归档本阶段，或回复具体页面/状态问题以便新增 gap closure 计划。</resume-signal>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                    | Description                                                  |
| ------------------------------------------- | ------------------------------------------------------------ |
| audit script → CI/local quality gate        | 门禁需要准确识别旧视觉回流，而不能把合法语义状态全部打成噪声 |
| final baseline refresh → release confidence | 错误的最终快照会把视觉回归固化成新的“正确”输出               |

## STRIDE Threat Register

| Threat ID  | Category | Component                                 | Disposition | Mitigation Plan                                                      |
| ---------- | -------- | ----------------------------------------- | ----------- | -------------------------------------------------------------------- |
| T-999.2-22 | T        | scripts/check-visual-hardcoded-colors.cjs | mitigate    | 针对旧蓝灰主视觉模式做精确规则与 allowlist，避免门禁被噪声绕过或失真 |
| T-999.2-23 | T        | tests/visual/baseline.spec.ts             | mitigate    | 最终基线只允许从 fixture route 刷新，禁止再依赖运行态条件分支        |
| T-999.2-24 | R        | phase acceptance                          | mitigate    | 增加 blocking human verify，确保视觉判断不完全委托给自动化截图 diff  |

</threat_model>

<verification>
- `npm run lint:design-md`
- `npm run typecheck && npm run build && npm run test:visual:update && npm run test:visual`
- Human verification checkpoint completed
</verification>

<success_criteria>

- 999.2 结束后有自动化 hardcode 回归门禁。
- 最终视觉基线来自稳定夹具并覆盖关键页面。
- 人工确认 DESIGN.md 的品牌、层级、hover 与 light/dark 一致性达到可接受状态。
  </success_criteria>

<output>
After completion, create `.planning/phases/999.2-design-md/999.2-10-SUMMARY.md`
</output>
