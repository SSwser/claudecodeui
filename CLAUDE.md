# CloudCLI UI — Claude Agent Guide

> See [AGENTS.md](./AGENTS.md) for the full project guide (applies to all AI tools).
> This file adds Claude Code CLI-specific context only.

## Reading Order

1. Read `AGENTS.md` in the repo root — contains all project conventions, GSD workflow policy, and architecture rules.
2. This file — Claude Code CLI-specific overrides (none currently; file is reserved for future Claude-only rules).
